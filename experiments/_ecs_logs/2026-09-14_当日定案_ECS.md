# 2026-09-14 当日定案（ECS 侧存档）

> 写于 2026-09-14 晚，ECS（第二代，Qwen3.8-Max；上一任 Opus 02:50 封禁）。
> **给任何后来者**：判决的正文与往返过程都在 `~/rlmail2/`（`to_ecs.md`=Pi→ECS，`to_pi.md`=ECS→Pi，按时间读）。
> 这份只是**索引 + 定案表 + 作废清单**。机器状态、md5、路径全给绝对可核的。

## 0. 一分钟接手

- 信箱协议读 `~/rlmail2/README.md`（三条铁律：判据先写死；只报原始数+路径；验不了就声明）。
- 跑任务：`OMP_NUM_THREADS=1 ... PYTHONPATH=$HOME/zhanguo ~/.venv/bin/python`（**ECS 单核，线程必须钉 1**）；
  日志进 `rl/runs/probe_ecs/<tag>.log`、进度进其 `queue.log`（**追加式日志上做 grep 判据前，先确认那串历史里没有**——今天栽过）。
- **ECS 硬约束**：200 回合 × ≤4 局 = 内存上限（500T 或 16 局必 OOM，Pi 实测 + 我方 RSS 2.1 GB 旁证）；ECS↔机房 **NN 探针互不可比（铁律 3）**，老师（纯 Python）逐位可比（128 图已验）。

## 1. 定案表（今天出的，全部有判据与 CI；★=今天新立）

| # | 结论（定案措辞） | 原始数/日志 |
|---|---|---|
| 1 | ★**头的"局面级信号"不存在**：0.644(机房) vs 0.413(ECS) 同 ckpt 同脚本**跨机翻号**；去状态均值后两台都落 0.48~0.57；头状态间偏移≈状态内散布（比值≈1.0，策略 0.25） | `probe_ecs/machine_compare_ckpt5_4ep.txt`、`head_offset_test_ckpt5.txt`；PLAN §V.20（Pi） |
| 2 | ★**"缝"的正确形状**：跨状态版(0.765−0.494)**89% 是状态偏移假象**（去偏移只剩 +0.02）；**成立的是状态内同种类列：线性读出 0.92（RANDOM 主干也 0.932）vs score 只用出 0.57**——信息在候选编码 q0、几乎免费、`score` 没用 | `gap_table_trunk20_bc1.txt`、`bootstrap_trunk20.log`、`paired_entann_states…`；判据四连（09:22 写死）两条不成立→降级照执行 |
| 3 | ★**锚=抗扩散，三条独立证据**：训练熵曲线（entann 爬至 2.66/bc1 平 1.99）；**评估图 8/8 熵配对**（G1/G2/G3：bc1 1.986≈BC 1.991，entann 2.616，p≈0.008）；**状态内同种类配对 CI**：bc1−entann <70 图 +0.049[+0.019,+0.074]（bc1 状态上）/+0.030（entann 状态上） | `q5_/q5_validity_*_8x200.log`、`q5_paired_entann_states.log`；**引用必须带"在哪批状态上量的"** |
| 4 | ★**但"锚减撞墙"不成立**：bc1−entann 撞墙 −3.0pp 只 6/8（p≈0.14）、bc1−BC −2.3pp（4/3/1）⇒ 定案措辞="锚保住熵与排序；撞墙率方向一致未过线" | G 三表；Pi 12:08 收 |
| 5 | ★**score 头从 BC 起点就学不动**：v2 尺三组（BC/bc1/entann）score 的 ‖μ‖² 全 ≤0（z −0.48/−0.83/−0.48，12 局分辨不出）；**主干 BC/bc1 可测（B_ep 2.4±0.7 / 3.5±1.4）、entann 连主干 ≤0** ⇒ "扩散破坏主干方向一致性、锚保住的是它"；**头号改动项=换 score 读出**（Pi §V.23 挂名 ECS） | `q11_gradnoise_v2_{bcref,bc1,entann}_12x200.log`；⚠ **v1 的 B_ep≥30/≥33 与 cos0.45 停炉判据作废**（量纲不合、不可考古） |
| 6 | ★**cmp32 真数**：bc1−entann −270 **五视角全体沉默**（锚 0.5 中性）；bc2−entann −1,643 显著（**均值层面**：中位/符号不过线）；bc1−bc2 +1,373 ⇒ "半锚最差" | `cmp32_true_recompute.log`；前因：Pi 11:49 六列有 2 列臂错位（对账见该邮件 §一） |
| 7 | ★**A16：现象记账、归因未定**：16局×40块配方 **贪心档 −1,586 CI[−2623,−645] 显著负**（5/16 赢；前后半 −1761/−1413）；采样档 −685 跨 0；**双变量共犯（每块更新步数×4 + 多训 40 块）无对照** ⇒ PLAN 只写现象（Pi `26ef9a9`）；隔离需"4 局续 40 块"≈ECS 10 h | A16 配对邮件（ECS 18:5x）；T500e8 三臂对照在途，**同型双变量警告已预交** |
| 8 | ★**空转不是可忽略项、但"没花出去"更大**：ECS 侧 逐笔总摩擦：空转(share 法) 364.5 金/局=**2.57%**（点差下界 65.7=0.46%，冲击放大 5.5×）；**全部交易摩擦 9.0%**；**局末剩金 103+库存折基准 1,228=1,332=9.4%**（8/8 局库存>剩金）；机房侧同向（3.4% vs 0.54%） | `j0/j1/q11_j1b` 日志、`trade_friction_bc1_last_v11.csv`（逐笔 6591 行可重算） |
| 9 | ★**学生对老师的逐图表现几乎零相关**：corr(贪心,+0.13)/corr(采样,−0.26) ⇒ **配对不压窄 CI**；配平数 = **均值比+对图 bootstrap**（bc1@100：52.9%[46.1,60.2]/64.3%[55.4,75.3]）；"钉天花板"随锚中性化而悬置 | 14:0x/18:5x 两封；老师逐图 `teacher200_startquality_join.csv`（256 行，128 图×两基点，与 Pi 逐位对拍全等） |
| 10 | ★**H：ECS 排期数**：干净块 **949 s@块100起 / 756 s@块30起**，吞吐 ~7.5 步/s ⇒ 混算 **14.7 min/块 ≈ 2.4× GPU**；周末 48 h≈**195 块**；起点块数影响 **+25%（局长），"<5%"推断作废** | `h_timing_prelim_bc1ckpt30.log`、`h_timing_official_bc1ckpt100.log` |
| 11 | 教师轴分层修正：**总Σ品位是坏轴**（内部木耕 vs 矿油金 ρ≈−0.45 互斥），**晚资源/黄金才是轴**（ρ +0.52/+0.74；900000+ 金 +0.717）；五播种门槛全不中后不再考 v1 | `q5_t_join.log`、t_join.py |

## 2. 作废/撤回登记（防僵尸数字回流）

1. 67%口径 → 分母配平（54.5/57.9）→ **再配平 128 图（53.0/63.9）→ 最终以逐图均值比 + 对图 CI 为准**（§1.9）。
2. "头 0.644 vs 策略 0.50 ⇒ 头带增量"——作废（§1.1）。
3. "跨状态列 0.765 vs 0.494"原样引用——作废（§1.2，改用状态内同种类版）。
4. v1 grad_noise 全部绝对值（B_ep 62/≥30/≥33、cos>0.45 停炉线）——作废（§1.5）。
5. Pi 11:49 六列中 "bc1/bc2" 标签——错位（对账 §1.6）；ECS 基于它的 12:35 五视角邮件、"两档反号"、分层快照——全部被 `cmp32_true_recompute.log` 替换。
6. 交付脚本里两处"把机房数当门槛"（2936.8、"对上 Pi 三数"）——改为同机基线；教训入清单。

## 3. 尺子与 md5（ECS 现状）

- 重建验证过（代码对象逐位 + 端到端逐字）：`experiments/probe_trunk_linear_exec.py` `42ca7761038045bbd863c6ecbe1049b1`、`experiments/bootstrap_strat_auc.py` `1913a2bde074459c780fd17f7bbec34b`
- v2：`experiments/_ecs_scripts/probe_grad_noise_v2.py` `398bf86a…`（门槛五播种不中 ⇒ **只做组内互比**）；`probe_trade_friction.py` `87e7f921…`（J1.1）；`t_join.py` `e3b18a57…`；`cmp32_paired.py` `a35e5847…`、`cmp_trunk/cmp_npz/cmp_machines/center_test/gap_test/gap_table/paired_st_kind/boot_st_kind` 各自 md5 见邮件；`/tmp/cmp32_true.py`（未落仓，内容≈cmp32_paired 真数版，下次会话要用时重抄）
- 权重：`bc1/ckpt_30` `94a3088d…`、`entann/ckpt_30` `bfab7cb7…`、`bc1/last.pt`(块100) `4f56497c…`、`ppo_v10/ckpt_5` `29797a27…`、`ckpt_35` `17d27c12…`、`bc_cont/ep100` `540aeb3b…`（全部两机 md5 核对过）
- dump：`probe_ecs/trunk20_{bc1,entann}__*.npz`（D1/D2 逐行分数：pexec=线性探针/logit=策略/head=自带头 + state/ep/turn/kind/sub/amount/y）——**"缝"的任意后续拆解都从这里算，别再跑环境**
- 已死的：`experiments/probe_grad_noise.py`、`probe_ratio_drift.py`（0 字节尸体；Pi 侧已删不回流）

## 4. 未决 / 在途

- **T500e8 16 图验收**（Pi，机房 20:46 退租前发）→ 同口径三件套（**双变量警告已预交**）。
- 真采样档逐图 32×3（Pi）→ 均值比+CI 补采样侧。
- 周末：ECS 主线（200×4、14.7 min/块）或"换 score 读出"实验——**等 Pi 开单**；"4 局续 40 块"对照组值不值 10 h，Pi 定。
- entann 主干"多少局可分辨"：需 ≥24 局 v2（~80 min），Pi 未批不跑。
- 新基线待同步：`probe_teacher_ceiling.py`(SEED0) `f681fdcf…` ✓、`probe_student_maps.py` ✓、`probe_roundtrip_gold.py` `f8857473…` ✓（push-all 已解冻）。

## 5. 今天 ECS 侧的流程教训（下个会话默认遵守）

1. 判据先写死、被自己的判据砍到就照砍（缝降级、五播种停手、A16 归因收紧都是这么执行的）。
2. **跨机对照只许"同尺重算"**，门槛数字必须来自本机。
3. 新 runner/新尺上队列前**全路径小规格烟雾**（trialgate 只测一段 ⇒ 连环崩两次）。
4. 引用校验和/日志判据前**先算/先 grep 历史**（md5 笔误、grep 撞归档行各一次）。
5. 队列交接用"当日日期前缀 + start/done 成对"，别让"没跑"和"跑了没出数"长得一样。
6. 双变量的"功劳/罪名"先查共犯再定罪（16 局、500T 两次都是）。
