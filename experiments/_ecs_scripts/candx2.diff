--- a/rl/transformer.py
+++ b/rl/transformer.py
@@ -128,6 +128,10 @@
         self.to_q = nn.Linear(d_cand, d_model)
         self.cross = nn.MultiheadAttention(d_model, n_head, dropout=p_drop, batch_first=True)
         self.ln_c = nn.LayerNorm(d_model)
+        # ★candx2：候选互见用**独立**注意力层（不绑定 cross——Pi 20:04 审查：
+        #   候选→窗口 与 候选→候选 是两种关系，绑成一套权重会压住这一改想修的表达力）。
+        self.cross2 = nn.MultiheadAttention(d_model, n_head, dropout=p_drop, batch_first=True)
+        self.ln_cand = nn.LayerNorm(d_model)
         # 打分 = 注意力输出(d_model) ⊕ 候选自身编码(d_cand) —— 光有注意力输出
         # 会丢掉"这个候选自己是什么"（注意力的输出是窗口的一个加权和，不带候选身份）。
         self.score = nn.Sequential(nn.Linear(d_model + d_cand, d_model), nn.GELU(),
@@ -214,9 +218,17 @@
         q0 = self.cand_mlp(torch.cat([te, se, ae, pos, ag, ahas], dim=-1))   # [B,K,dc]
         qq = self.to_q(q0)                                                   # [B,K,d]
         # Q = 候选（K 个），K/V = 窗口（T 个）—— `batch_first=True` 下序列维就是 K。
-        # 代价 O(K·T·d)，**不是** O(K²)：候选之间互不可见，这是明知的不做。
+        # O(K·T·d)。candx 改动：在此之后**再让候选互相看一轮**（见下），不再是"明知的不做"。
         att, _ = self.cross(qq, x, x, key_padding_mask=kpm, need_weights=False)
         h = self.ln_c(att + qq)          # 残差：注意力输出 + 候选自己
+        # ---- candx2：候选互相可见（独立层） ----
+        # Q/K/V 全用 h，但走**新参数 cross2**；新增 = cross2(148,224) + ln_cand(384) = +148,608（+7.0%）。
+        # key_padding_mask 用 cand_mask 取反：padding 候选不许当 K/V 被看见（它们那行是垃圾）。
+        # 成本是**叠加**不是替换（Pi 更正后的口径）：O(K·T·d) + O(K²·d)。
+        # 语义：动作选择的分数从此可以表达"这个比那个好，因为那个此刻点不动/更贵/已被引用"。
+        cm = None if cand_mask is None else ~cand_mask.to(h.device)
+        att2, _ = self.cross2(h, h, h, key_padding_mask=cm, need_weights=False)
+        h = self.ln_cand(att2 + h)       # 残差：互看输出 + 各自原表示；exec_head 输入随 score 同步（保持"同一份 [h,q0]"不变式）
         logits = self.score(torch.cat([h, q0], dim=-1)).squeeze(-1)
         if cand_mask is not None:
             # ★`.to(logits.device)` 是**防御**，不是多余：`cand_mask` 由 collate 产生，
