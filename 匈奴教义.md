# 匈奴教义（保密策略）

> 本文档是**匈奴政体的人类侧策略说明**，与主 README 分开存放：主 README 不写具体
> 打法，普通国家（AI）在游戏内也**查不到**这段策略——教义只注入匈奴自己的
> system_prompt / rules（按 polity=huns 判断），普通国调用 rules 不会返回。
>
> **2026-09-09 修订**：重心从「小额索赔续命」改为「**快速夺取金矿与补给厂**」；
> 勒索价码下调到真能到手的量级、不再夸大兵威；新增「只跟能谈的谈，打疼了再说」
> 与「坚壁清野型全力摧毁」两条；所有金/补给数字已按同日市场改革
> （沿曲线均价结算 / 10% 买卖价差 / 供需均衡价）重算。

## 登场方式

- **新局**：待加入国（如「林胡」条目带 `polity: huns`）在 **enable_turn 120–150 回合窗口**内
  随机一回合自动登场；窗口见 `mp_config.json` 里该条目的 `enable_turn` / `enable_turn_max`。
- **旧存档**：不看 enable_turn，不自动登场——需要你手动在看海终端输入 `add 林胡`（或 `add 匈奴`）。
- 手动 `add 国名 匈奴` 随时可加（随机到距各国足够远的位置）。

## 政体参数（开局）

下表是**默认值**；配置条目里可用 `start_cavalry` / `start_gold` / `start_supply` 定制
（如楼烦即 8 骑 / 2000 金 / 300 补给），匈奴自己的 system prompt 不写死具体数字。

| 项 | 默认值 |
|----|----|
| 骑兵 | 6 支（骑·100HP·动2格·每回合耗 2 补给/骑） |
| 黄金 | 1000 |
| 补给 | 200（6 骑每回合耗 12 → 约 16 回合的口粮，**必须靠抢/勒索/买补**） |
| 建筑造价 | **+30% 惩罚**（不擅建设，别走种田流） |
| 骑兵征召 | **8 粮 + 8 装**（比常规 12+12 便宜） |
| 外交 | **禁止**结盟/共同防御/保障独立/馈赠/交换地图 |
| 外交可用 | `send_letter`（勒索贡品）/ `spy`（100 金刺探）/ `declare_war` / `offer_peace`（逼降/赔款）/ `accept_peace` / `reject_peace` |

## 生存方式（核心）：抢产为生，不是勒索为生

- 你的口粮来自**夺来的产能**，勒索只是零钱。
- **每座金矿 = 10 金/回合 ≈ 1 骑的口粮**。骑每回合耗 2 补给；补给市场买价
  ≈5.3 金/单位（含 10% 价差，大批买还会把市价推上去），所以 1 骑 ≈ 11~12 金/回合。
  6 骑 ≈ 65~70 金/回合 —— **6 座金矿才养得起 6 骑**。
- **每座补给厂 = 2 补给/回合 = 1 骑的口粮**，但它要 **1 粮 + 1 矿 + 1 电**配套。
  抢补给厂必须连它的农场/矿场/能源厂一起拿下（或先攒够粮矿），否则抢来就是一块空转的地。
  → 因此**金矿优先、补给厂并列**：金矿零输入、直接出金，黄金能在市场上买任何东西。
- **时间是敌人：越拖越容易死**。开局 200 补给 = 16 回合的命，之后每回合都在流血，
  而对方在种田、在发育、在筑城。**必须在补给见底前把「存量」变成「流量」**——
  抢到能持续产金/产补给的地，你才活得下去。
- 还有一条更硬的约束：**你开局粮/木/矿全为 0**，连一座建筑都盖不起（何况还有 +30% 造价惩罚）。
  所以你的每一分物资都只能来自**抢、勒索、市场买**——这三条腿里，只有"抢"是可持续的。
- 抢地优先级：**金矿地 > 补给厂地 > 粮/木/矿产地 > 无驻军空城 > 有驻军要塞（最后，能不碰就不碰）**。

## 勒索与诈骗：价码要小、要真

- **一次要 50~100 金 + 5~20 补给为宜**。这是「对方懒得为它开战」的量级：
  张口几百金，对方宁可宣战；要个 80 金 + 10 补给，多数国家顺手就给了。
- **不要过度夸大兵威**：对手可以花 100 金 `spy` 你，拿到的是**各兵种的真实数量**
  （只看数量、无位置），战报与相邻视野也会露馅。把 6 骑吹成 20 骑，只会让整封信
  不可信、直接被拒。虚张声势的正确用法是**不报具体数字**（"塞外铁骑已至城下，各城自择"），
  而不是报假数——报价可信，钱才收得到。
- 勒索是顺手牵羊，不是主粮：到手立刻换补给，别指望靠它活。
- **别对同一国家反复勒索**：第二次它给得更少，第三次它开始备战。勒索对象要轮换，
  或者一次要够（但别超 100 金）——见「我的理解」第 5 条。

## 运动战教义（必须贯彻）

1. 骑兵快（2 格）：集中、打完就撤；**不恋战**。撤离用 `retreat`（固定只能退相邻 1 格、耗移动，
   回合末随战斗结算后脱离；防御方撤退减伤 50%）——`mv` 不能从交战地撤离。
2. **骑兵集中决战可以轻易战胜敌方总量多很多的军队**——你总可以以少打多：
   - 集中骑兵，挑**软柿子**（敌方分散 / 单点兵力少 / 补给差 / 无城堡）打局部决战；
   - 避免硬拼满员要塞（山地+城堡防御相乘叠加可达 75% 减伤，正面硬打是送）。
3. **少打有损失之战**，力求**百战百胜**：能骗到就别打、能抢空城就别打硬仗。
4. 抢下来的地要**守**——但分兵守地会削弱你唯一的拳头（见「我的理解」第 1 条）。

## 议和：只跟能谈的谈，绝不亏本

- **拒绝谈判的国家不要议和**：对方 `reject_peace` / 只肯白和 / 干脆不回信 →
  **别再发第二次，打疼了再说**。继续占它的产金地、灭它的野战军；它疼了会自己来求。
  反复去求一个不想谈的国家，只是把回合浪费掉。
- **坚壁清野型**（不议和、不给钱、不还价、油盐不进）：别在它身上费力气，
  **全力摧毁**——不打要塞，专拆它**无驻军的产金/产补给地块**，一块一块吃干净，
  直到它亡国或服软。它不给你钱，你就拿走它的产能。
- 议和先算账：`索款(demand) 必须 ≥ (这仗已打的回合 + truce 停战回合) × 每回合军费`
  ```
  每回合军费 = 骑兵数 × 2 补给 × 补给买价(≈5.3 金，含价差)   ← 6 骑 ≈ 65~70 金/回合
  ```
  算不过账就把 `truce` 压到 0~2 回合，或干脆不议和、继续抢——反正你靠抢活着。
- 赔款只能拿黄金 → 到手第一件事 `buy` 补给（一次买够未来 10 回合的量），别攒黄金。
  补给仓空 = 每军按缺口比例扣血（满缺 -35HP/回合，交战中也照扣），几回合就死光。

## 实战节奏建议

1. `countries` / `spy` 选目标：**有金矿或补给厂、且守备薄**的国家优先（spy 100 金，
   3 回合后拿到对方国库/建设底细 + 各兵种数量 + 地图，值这个钱）。
2. 能骗就骗（50~100 金 + 5~20 补给）；骗不到就 `declare_war`。
3. 开战后**直奔产金/产补给地块**，不打要塞；占了就守，逼它来救（来救正好野战歼灭）。
4. 打疼了再 `offer_peace` 索款；对方拒谈 → 继续吃它的产金地，**别回头求它**。
5. 金/赔款到手 → 立刻买够 10 回合的补给。断补给是匈奴大忌。

## 我的理解与先验（Claude 补充，2026-09-09）

1. **抢产与集中兵力是一对矛盾**：匈奴强在集中骑兵打歼灭战，弱在守不住地。
   抢来的金矿若在对方腹地，分兵去守就削弱了拳头。→ 正确顺序是：
   **先用主力把对方野战军打残（不是打城）**，让它无力反扑，再从容占产金地；
   产金地尽量选**靠自己方向、成片**的，别抢一块四面皆敌的飞地。
2. **金矿比补给厂更值**：金矿零输入、10 金/回合，且黄金能买补给也能征召骑兵（8粮+8装）；
   补给厂要粮/矿/电三条链，抢来常常缺配套。除非那座补给厂坐落在粮矿富地，否则先抢金矿。
3. **别囤金**：黄金只有两个用途——买补给、征召骑兵。囤到 500 金却饿死 3 骑，是最蠢的死法。
   手上常备 3 回合的补给（6 骑 = 36）再谈开战。
4. **打不动就换人**：匈奴是机会主义者。一个国家啃不动（要塞多、兵多、坚壁清野），
   换下一个目标远比硬啃划算——你的时间只够打穿一两个国家，别把命耗在城墙上。
5. **勒索的边际收益递减**：同一个国家被勒索两次以上，第三次要么给得更少、要么直接备战。
   所以对象要轮换，或者一次要够（但别超 100 金，超了就不给）。
6. **「打疼了再说」比「打赢就谈」更赚**：占它一座金矿，比多要 100 金赔款值——
   金矿每回合产 10 金、一直产；赔款一次性、还可能换来对方喘气筑城。
   只有当你**守不住**那块地（对方主力尚存、离你太远）时，才退而求其次收赔款。
