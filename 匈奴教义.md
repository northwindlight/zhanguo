# 匈奴教义（保密策略）

> 本文档是**匈奴政体的人类侧策略说明**，与主 README 分开存放：主 README 不写具体
> 打法，普通国家（AI）在游戏内也**查不到**这段策略——教义只注入匈奴自己的
> system_prompt / rules（按 polity=huns 判断），普通国调用 rules 不会返回。

## 登场方式

- **新局**：待加入国（如「林胡」条目带 `polity: huns`）在 **enable_turn 120–150 回合窗口**内
  随机一回合自动登场；窗口见 `mp_config.json` 里该条目的 `enable_turn` / `enable_turn_max`。
- **旧存档**：不看 enable_turn，不自动登场——需要你手动在看海终端输入 `add 林胡`（或 `add 匈奴`）。
- 手动 `add 国名 匈奴` 随时可加（随机到距各国足够远的位置）。

## 政体参数（开局）

下表是**默认值**；配置条目里可用 `start_cavalry` / `start_gold` / `start_supply` 定制
（如楼烦即 8 骑 / 2000 金 / 300 补给），匈奴自己的 system prompt 不写死具体数字。

| 项 | 默认值 |
|----|----|
| 骑兵 | 6 支（骑·100HP·动2格·每回合耗 2 补给/骑） |
| 黄金 | 1000 |
| 补给 | 200（6 骑每回合耗 12 → 约 16 回合的口粮，**必须靠勒索/抢/买补**） |
| 建筑造价 | **+30% 惩罚**（不擅建设，别走种田流） |
| 骑兵征召 | **8 粮 + 8 装**（比常规 12+12 便宜） |
| 外交 | **禁止**结盟/共同防御/保障独立/馈赠/交换地图 |
| 外交可用 | `send_letter`（勒索贡品）/ `declare_war` / `offer_peace`（逼降/赔款）/ `accept_peace` / `reject_peace` |

## 生存方式（核心）

- 不靠种田建厂活，靠 **勒索 + 抢地 + 胜仗赔款 + 市场买卖** 维生。
- **勒索优先要补给**：你缺补给（6 骑 × 2 = 12/回合），写信点名要补给/粮食，其次要金；不给就打。
- 抢地优先 **无驻军之地**（atk 空城即占），见缝插针，避免硬啃有重兵的要塞。
- 战后（尤其是打赢）**立刻**写信要求对方投降 / 赔款（offer_peace pay=索款/demand=你索/white 白和），别拖。

## 运动战教义（必须贯彻）

1. 骑兵快（2 格）：集中、打完就撤；**不恋战**。撤离用 `retreat`（固定只能退相邻 1 格、耗移动，
   回合末随战斗结算后脱离；防御方撤退减伤 50%）——`mv` 不能从交战地撤离。
2. **骑兵集中决战可以轻易战胜敌方总量多很多的军队**——你总可以以少打多：
   - 集中骑兵，挑**软柿子**（敌方分散 / 单点兵力少 / 补给差 / 无城堡）打局部决战；
   - 避免硬拼满员要塞（山地+城堡防御相乘叠加可达 75% 减伤，正面硬打是送）。
3. **少打有损失之战**，力求**百战百胜**：能勒索到就别打、能抢空城就别打硬仗。
4. **虚张声势**：明明只有 6 骑，写信要说成 10 骑甚至更多，夸大兵威逼人交钱/交补给。

## 议和经济学（**绝不能亏本议和**）

匈奴最常见的死法：**打赢收了一笔小钱就停战，然后整个停战期把全军饿死**。
停战期内对方一分钱不会给你，但你的骑兵照吃补给——**停战不是收入，是纯支出**。

议和前先算这笔账（`offer_peace` 的 `truce` 参数由双方自行约定，0=不休战）：

```
索款(demand) 必须 ≥ (这仗已打的回合 + truce 停战回合) × 每回合军费
每回合军费 = 骑兵数 × 2 补给 × 市场补给价（≈5 金）   ← 6 骑 ≈ 60 金/回合
```

- 例：6 骑、停战 10 回合 → 光停战期就要 600+ 金（大批买补给还会把价推高，实际更贵）。
  打赢一场仗只勒索到一两百金就停战 = 必亏，10 回合后全军饿死。
- 算不过账就别谈：**把 `truce` 压到 0~2 回合**，或干脆不议和、继续抢——反正你靠抢活着。
- 赔款只能拿黄金，所以**拿到钱第一件事是 `buy` 补给**（一次买够未来 10 回合的量），别攒黄金；
  补给仓空 = 每军按缺口比例扣血（满缺 -35HP/回合，交战中也照扣），几回合就死光。
- 同理：勒索(`send_letter`)时优先点名要**补给**（实物，直接吃），其次才要金。

## 实战节奏建议

- 第 1 步：`countries` 选目标 → `send_letter` 威吓勒索（点名要补给+金，可虚报兵力）。
- 第 2 步：不给 → `declare_war`，用骑兵集中攻其**无驻军/兵力少**的地块（atk 含占领），打完就撤或继续。
- 第 3 步：拿到胜果（占了地/打残其军）→ `offer_peace` 索要赔款/投降，**按上面的公式算够本再谈**，见好就收。
- 断补给是匈奴大忌：补给仓空 = 每军按缺口比例扣血（满缺 -35HP/回合），务必勒索/购买补给保命。
- 情报可用 `spy`（100 金）看目标国库/建设，决定勒索价码与打谁。
