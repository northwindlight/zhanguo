# 地形改版设计：柏林噪声 + 水系 + 渡河 + 机动限制

> 状态：**设计定稿，暂缓实施**（2026-09-08 用户逐项拍板）。
> 本文档 + `tools/terrain_preview.py`（可复跑原型）保存在本分支，实施时照 §7 清单开工。

## 0. 背景与目标

- 痛点 1：现地形是纯随机撒盐（权重表），地貌无地理逻辑。
- 痛点 2：**骑兵进敌国腹地如入平原**——步兵填线、骑兵无解，地形对机动零影响。
- 目标：噪声大陆 + 不可通过的水（海/河）+ 渡河方式 + 地形限制机动。

## 1. 噪声地形（已选型实测）

- **库：`opensimplex` 0.4.5.1**（纯 Python + numpy 向量化；种子确定性已验；requirements 需加一行）。
  OpenSimplex2 是 Perlin 改良系（无网格伪影），地形用途优于经典 Perlin；备选 perlin-noise（逐格循环，大图慢）。
- 双 fBm：**海拔 e**（4 octave）+ **湿度 m**（3 octave），频率 scale=0.09（特征约 11 格），振幅逐层折半。
- 归一化后分带（50×50 四种子实测陆地 **79-83%**，满足 ≥80% 要求）：

| 条件 | 地形 |
|---|---|
| e < **0.30** | 水（海/湖，见 §4 未决） |
| e > 0.78 | 山地 |
| m < 0.33 | 沙漠 |
| m > 0.62 | 森林 |
| e > 0.58（其余） | 丘陵 |
| 其余 | 平原 |

- 海平面扫线记录：0.36→70-74%，0.33→74-79%，**0.30→79-83%**，0.27→83-88%。
- `terrain(seed, x, y)` 纯函数契约不变（视野预览/战报/未激活格都依赖）；全图生成 <0.6s（开局一次性）。

## 2. 河流生成

- 源头 = 海拔前 8% 分位（且 ≥ SEA+0.3），最多 size/12 条，源头间隔 ≥5 格，路径 ≥8 格才成河。
- 逐格走最低下坡（8 邻，平局按坐标序，visited 防打转），入海或内流盆地止。
- 河刻 1 格宽，**每 7 格 1 个渡口 + 河尾保底**（每条河 2-4 个）。
- **用户反馈（未解决）：河流偏少**——目前 2-4 条/50 图，实施时加大（降源头分位至前 12%、提高条数上限、允许更短路径）。

## 3. 渡河与机动

- **渡口**：可停留过河（站河上 1 回合、下回合上岸），防御 −25%，无主公共、不可建不可占——半渡而击的战场。
- **移动路径化**（前置）：mv/atk 多格移动逐步走、每步查可通过——顺带修掉现有"骑兵跳格不查路径"漏洞。
- **骑兵机动表**（已拍板）：

| 兵种 | 平原/沙漠 | 森林/丘陵 | 山地 | 渡口/桥 |
|---|---|---|---|---|
| 步兵 | 1 格 | 1 格 | 1 格 | 1 格（停留） |
| 骑兵 | **2 格** | **1 格（崎岖降速）** | **禁入** | 1 格 |

- 战斗数学不动；冲锋失效惩罚（骑兵从/向森林丘陵 atk −20%）挂起观察。

## 4. 水域规则与未决项

- 水格：不可占领/不可建/无产出/无野人；河流仅渡口与（未来）桥可入；海洋=大地图专属
  （sea_level 随 size 缩放，小图无海）；港口跨海=远期。
- **用户反馈（未解决）：海湖不分**——内陆低地自然成的水体与海洋同名同符号（~），观感差。
  实施时区分：内陆水体（不与海连通）单独定名「湖泊」并给独立字符（如 `o`/`O`），
  湖是否给邻格资源加成（淡水）另行讨论。
- 桥梁属性（永久公共/有主/可拆）= 用户"再讨论"，挂起。

## 5. 已接受权衡

- 8 向相邻下河拐弯处斜角可切（渡口密度兜底，实测不行再收紧正交）。
- 渡口不可摧毁不可占——河防只能"堵"不能"断"。
- 山地禁骑精准削弱纯骑兵国家（匈奴）→ 教义补"避开山地/走平原走廊"条款（配套改动）。

## 6. 存档兼容

- 存档加 `terrain_gen: "random"|"noise"` 标记：旧档继续旧随机地形（否则旧档未激活格会滚出与邻居
  格格不入的新地形）；新局才用噪声。
- 河流/渡口/桥列表随档持久化（生成期一次性产物 + 桥是动态的，不能靠纯函数重算）。

## 7. 实施清单（到时候按序）

1. `game.py`：opensimplex 依赖 + 双 fBm 分带 + 河流/渡口/湖泊生成（一次算好缓存/存档）
2. `mp.py`：路径化 mv/atk + 骑兵限速/禁骑 + 水格拒绝（frontier/can_enter/retreat/guardians/activate）+ 布点连通性校验（十字全陆 + 陆块 ≥30 格）
3. `mp_ai.py`：rules 机动表 + 地图新字符（`~`海 `o`湖 `≈`河 `▲`渡口）+ land 面板孤立陆块警告 + 匈奴教义补丁
4. `README.md`：地形/机动章节重写
5. 存档：`terrain_gen` 标记 + 水系列表持久化

## 8. 原型工具

`tools/terrain_preview.py`：复现本文全部参数的渲染/扫线工具（`--seed --size --sea --rivers`，
`--sweep` 出海平面-陆地占比表）。本设计的所有数字都可用它复现。
