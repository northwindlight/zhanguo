# 训练机器性能简报（2026-09-11）

用于 `rl/` 那套 PPO/BC 训练的算力选型。**两台可选机器都是 CPU-only、无 GPU。**

> ⚠️ **Windows 生产机已退出训练**（2026-09-11）。原先 `train_bc.ps1` / `fix_bc.ps1`
> 指向它，那两个启动器现在没有可跑的机器了。本文件里的两台是仅剩的选择。
> （Windows 机仍是 `www.northwind.site` 的生产服务器，只是不再承担训练。）

---

## 两台可选机器

| | ECS（阿里云） | Pi 5（本地） |
|---|---|---|
| CPU | AMD EPYC, **1 物理核 / 2 线程(SMT)** | Cortex-A76 **×4 真核**, 无 SMT |
| ISA | AVX-512 + **AVX512-BF16** + VNNI | NEON |
| 内存 | 3.67 GiB（可用 ~3.3 GiB） | 15 GiB（可用 ~8.8 GiB） |
| 交换 | 4 GiB（zram 2G zstd + swapfile 2G） | 2 GiB（已用满） |
| 磁盘 | 40 GB NVMe（余 32 GB） | — |
| 内存带宽 | 55.6 GB/s 读 | 12.3 GB/s 读 |
| 内存延迟 | 与 Pi 基本相同 | 与 Pi 基本相同 |

## 环境

- 两台都是 **Python 3.13.5 + torch 2.14.0+cpu**。
- ECS 用 `~/.venv`（已含 numpy 2.5.3）；Pi 用 `~/projects/python/zhanguo/.venv`。
- PyTorch CPU 版训 Transformer 比入门独显慢 100~1000×，**只适合小模型 / 流程验证**。
- ECS 的 pip 走阿里云镜像；**GitHub 从 ECS 不通**，拉东西要走 Pi 中转。

## 关键基准

**CoreMark**（标准 CPU 基准）

| | 单线程 | 多线程 |
|---|---|---|
| ECS | 36,370 | 52,992（2T，**SMT 仅 1.46×**） |
| Pi 5 | 17,958 | 70,212（4T，**3.91× 近线性**） |

**Transformer 训练实测**（1 层 / d_model=64 / 4 头 / seq=64 / batch=2，8.6 万参数，
完整 fwd+bwd+AdamW）

| 精度 | ECS | Pi 5 | ECS 优势 |
|---|---|---|---|
| fp32 | 59,890 tok/s | 10,202 tok/s | **5.9×** |
| bf16 | 48,617 tok/s | 446 tok/s | **109×** |
| fp16 | 10,377 tok/s | 5,046 tok/s | 2.1× |

## 训练必须知道的 5 件事

1. **bf16 只能在 ECS 上用。** ECS 有 AVX512-BF16 硬件；Pi 无 bf16 指令，靠软件模拟，
   慢 109 倍。混合精度训练一律在 ECS 上开 bf16。

2. **ECS 的第二个 vCPU 在向量计算上收益为零。** 实测 SMT 对 fp32/fp64/bf16/int 的
   加速比全是 **1.00×**（FP 单元单线程已吃满）。CoreMark 的 1.46× 来自标量/分支负载。
   **按 1 核算力规划。**

3. **小模型加线程会变慢。** 实测 Pi 4 线程比 1 线程慢 **3.7×**，ECS 2 线程也慢于
   1 线程——小算子并行调度开销压过收益。**务必实测 `torch.set_num_threads(1)` vs
   多线程，别默认用满。** 模型越大，多核收益才显现。

4. **内存是硬天花板。** ECS 只有 3.67 GiB，训练态占用 ≈ 参数量 × 16 字节
   （fp32 权重+梯度+2 个 Adam 动量）再加激活值，**大约只能训到 1 亿参数出头**
   （且需短序列、小 batch）。超出请换 Pi（15 GiB），但要接受不能 bf16、单核慢 5.9×。

5. **fp16 两边都别用。** ECS 无原生 fp16（比它自己的 fp32 还慢 6×）；Pi 有原生 fp16
   但仍慢于自己的 fp32。**要省内存就用 bf16（仅 ECS），否则用 fp32。**

## 选型建议（原始）

- 模型状态 < ~2 GB 且要 bf16 → **ECS**，并开 `torch.autocast('cpu', dtype=torch.bfloat16)`
- 模型更大 / 需要大 batch 长序列 → **Pi 5**，用 fp32（没有 bf16）
- 真要训练 → 上 GPU，这两台都别考虑

---

## 对本项目 RL 的含义（Claude 补，2026-09-11）

上面那份是纯机器基准。落到 `rl/` 的具体计划上，有三条会**改结论**：

### ① 成本锚点要换：Windows 机没了，改用 ECS

`rl/README.md` 里「一轮 PPO（8 局 + 更新）在服务器上约 10~15 分钟」是**在 Windows 机上**
量的，那个锚点作废。按上表重锚（Pi→ECS 的 fp32 训练比 = 5.9×）：

| | Pi 5 | ECS |
|---|---|---|
| 现状（0.13M 点积头）一局更新 | ≈ 6 分钟 | **≈ 1 分钟** |
| 512-token / 3M 方案（42×） | ≈ 4.2 小时 | **≈ 42 分钟** |

→ `rl/TOKEN_DESIGN.md` 第 6 节那些倍数关系不变，**绝对时间按 ECS 这一列读**。
一个月的训练预算下，42 分钟/局 ≈ 34 局/天 ≈ 1000 局，**可行但紧**。

### ② rollout 缓冲必须存**帧**，不能存 token（这是硬约束，不是优化）

ECS 可用内存只有 3.3 GiB。512 token × d192 存 fp16 = **196 KB/步**，
一局约 1.2 万步 → **2.4 GB**，装不下。

→ 训练时必须**存原始帧（bbox 网格 + 候选特征，~60 KB/步 ≈ 720 MB）**，
更新时**重建 token 窗口**。这条本来只是"更省"，在 ECS 上变成"唯一可行"。
Pi 上（8.8 GiB 可用）也建议照做，两边同构才不会各跑一套。

### ③ bf16 与线程数：都别照抄建议，要实测

- 简报正文推荐 ECS 上开 bf16，**但它自己那张表里 bf16（48,617）比 fp32（59,890）慢**。
  那是在 8.6 万参数的玩具上测的；我们的 3M 模型 + 512 序列矩阵大得多，bf16 大概率反超，
  且省一半激活内存（对 3.3 GiB 很值）。**结论：在真实模型上量一次再定，别外推。**
- 线程数同理：简报第 3 条说小模型多线程更慢。ECS 只有 1 物理核，本来就没什么可并行；
  Pi 上 `rl/train.py` 现在是"采样期切单线程、更新期切 4 线程"，**这条要重新量**。

### 没变的结论

- **几百 token 的窗口仍然是对的**，3.4k / 16k 仍然不可行——量级差没被动摇。
- 这两台都只能做**小模型 + 流程验证**。真要训 L6-L7 那个量级，得先解决算力，
  而不是在架构上再省。
