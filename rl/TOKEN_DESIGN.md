# RL 观测 token 化设计

面向「带记忆的注意力策略」的观测布局。目标不是"更强的网络"，是**把游戏逻辑里
现在表达不了的运算（排序 / 取最近 / 成组）变成可表达的**，同时把成本钉在 CPU
训得动的量级上。

前置约束（用户口径）：**引擎与 main 逐字相同；外交/战争在引擎里都在，
只是还没进 RL 的动作空间与观测**（会玩 → 外交 → 战争）。
（2026-09-12 之前的口径是「本分支 = main 减去外交」，那条已废弃 ——
砍外交对独局训练零收益，却要每次变基重做一遍手术。见 `rl/README.md`。）
所以布局必须**一次定死**，加外交时只填预留位，不换形状、不废 ckpt。

---

## 0. 为什么必须先冻结词表

本分支与 main 的枚举空间**已经不一致**：

| | main | feat/rl |
|---|---|---|
| `game.BUILDINGS` | **16**（含「外交中心」） | 15（外交中心已删） |

只要把建筑编成下标（`list(BUILDINGS).index(b)`），外交一回来、外交中心一插回去，
**所有下标全体平移**——地块通道、候选 `sub_idx`、embedding 语义一起错位，且不报错。

**第一版手写冻结表就栽在这**：凭印象把「外交中心」写在表尾，而它在 main 里落在
「瞭望塔」和「工程院」**之间**。`tests/test_vocab_main_parity.py` 第一次运行就抓到了
（它直接读 `git show main:game.py`，不读本分支的 `game.py`——那正是要防的东西）。

→ 一切下标取自 `rl/vocab.py`。**只许追加，不许插队、不许删。**

---

## 1. 窗口 token（self-attention 的对象）：固定 512

| 组 | 上限 | 字段（全数值，无字符串） |
|---|---|---|
| **G 全局** | 1 | 库存 7 `log1p`、现价 6、均衡价 6、消费 build/recruit/supply、回合/max、本回合步数 `log1p/3`、缺电、电量(有/需)、政体、领地数 `log1p/3`、建筑总数 16 `log1p/3`、军队(数/总HP/各兵种数)、**外交预留 8** |
| **M 地图 patch** | ≤64 | patch 内 `C×p×p` 拍平 → `Linear`；+ patch 中心 `(dx,dy)/POS_SCALE`；+ patch 内可见占比 |
| **A 军队** | ≤192 | 归属 3（自己/他国槽/野人）+ 兵种 3 + `hp/max` + `(dx,dy)/POS_SCALE` + 交战 + 本回合已动 + **情报年龄**（0=本回合可见，k=几回合前见的） |
| **N 势力** = **外交记忆** | ≤8 | 每国一个：关系 6 one-hot + 领土/军队/HP **现值 + 增量** + 接壤 + 上次观察回合 + 外交中心数 + 有无待议（见 §9.5） |
| **E 事件** = **事件记忆** | ≤32 | 按**回合**装填：类型 4 + 年龄 + actor 槽 2 + 落点 2 + 有落点 + 量级 + ok（见 §9.4） |
| **R 记忆** | 8 | ★**退役**（恒 mask、全零）—— 用户口径只有三类记忆，第 4 类"通用池化记忆"没有归属（见 §9.4 末） |
| **K 关键节点** → **地图记忆稀疏槽** | ≤16 | 原为"关键节点"，改作**值得单独钉住的格**：碰过壁的、曾属于我的、远处见过资源的（见 §9.3） |
| **合计** | **393** | 预算 512，留 25% 余量 |

> ★**2026-09-11 实现时的两处偏离**（`rl/tokenize.py` 的 docstring 里有完整理由）：
>
> 1. **M 组不用自适应 patch 边长 `p ∈ {2,4,8,16}`，改成固定 8×8 = 64 格、块边长自适应。**
>    因为 `C×p×p` 拍平后**宽度随 p 变**——同一次训练里不同帧的 token 宽度就不一样，
>    拼批时错位，P4 还得为每个 p 备一个 `Linear`。改成「块内逐通道均值」后宽度恒为
>    `C+3`，与地图/帝国大小无关，分辨率等价。代价是块内布局丢了。
> 2. **A 组宽度 12，不设"情报年龄"那一维**：`World` 没有「这块地何时探明」的记录
>    （只有事件日志 `history`，没有逐格探明时间戳）。与其填一个恒 0 的假特征，不如先不设。
>
> 另：**M 组的地块通道直接取自 `env._obs()` 的网格**（36 通道、已按视野门控、已裁到外接框），
> 不在这里重建一份。所以下面的 `C = 45` 冻结布局是 **P4 的账**（那时 CNN 退场、
> 通道表要冻结）；P2 阶段宽度是 `len(env.obs_channels()) + 3`。

**地图通道 `C = 45`**（冻结）：

```
地形 5 + 地块资源 5 + 归属 11 + 建筑 16 + 每格标量 8
归属 11 = 自己 1 + 国槽 8 + 中立 1 + 野人 1
每格标量 8 = 已用位/20、在建、本回合已建、我方HP/100、敌方HP/100、野人HP/100、边境、曾经探明
```

两个决定在这里钉死：

- **归属用固定 8 国槽**（`vocab.NATION_SLOTS`，槽位由国家码绑定）。
  **加国家不改通道数**——否则观测形状随局内国家数变，ckpt 直接废。
  实测配置是 5 国（秦楚齐燕赵），8 槽留了余量。
- **自适应 patch 尺寸**：`p ∈ {2,4,8,16}`，取"能塞进 ≤64"里**最小**的那个
  （分辨率尽量高）。12×12 外接框 → p=2 → 36 token；30×30 → p=4 → 64；
  100×100 → p=16 → 49。**token 数恒定，分辨率自适应** —— 替换掉现在
  "外接框随帝国长大"的变长做法。

---

## 2. 位置编码：不用绝对 PE

地图 / 军队 / 事件 token 的位置**以特征形式进入**（相对家的偏移 ÷ `POS_SCALE`），
**不加 learned/rotary 绝对位置编码**。理由与 2026-09-11 那次改动同源：绝对 PE 会把
"我在图哪个位置"漏回去，而那是智能体不可能知道的（迷雾挡着、从未探索过边界）。
坐标进特征还顺带保证平移等变。

**顺序必须是状态的确定性函数**：军队按 `id`、地块按 `(x,y)`、事件按 `(turn,seq)`。
不许用 dict 遍历序——仓库里已经踩过一次（地块命名搬出共享 RNG）。

---

## 3. 候选 token：不进窗口，走 cross-attention

候选数实测 **中位 132 / p90 890 / 峰值 1376**，塞进窗口就是 O(C²)。

```
候选(Q, ≤1376) ──cross-attn──> 窗口 token(K/V, ≤512)
   → MLP(注意力输出 ⊕ 候选自身特征) → logits
```

- 候选自身特征 = 类型 emb + 子项 emb + 数量 emb + 落点 `(dx,dy)` + 被引用军队的
  `(dx,dy,hp,kind)`。
- 代价 `4·C·K·d` ≈ **0.74 GFLOP**（窗口那部分的 ~19%），可接受。
- 收益：候选**看得见全局，也能在上下文里定位**——"派最近的兵去 X"里的"最近"，
  现在算得出来了。这正是现有点积头在函数类上缺的那一块。
- **候选之间仍然互不可见**（O(C²) 不做）：单挑型策略不需要，这是明知的不做。

**为什么现在这个头表达不了集合级运算**：`logits = q(s)·c(a)` 是双线性形式，
每个候选独立打分。它表达不了 v9（规则 AI）逻辑里最常见的三类运算——

```python
targets.sort(key=lambda p: TROOPS_FOR[...] / max(tile_info(p)[1], 1), ...)   # 排序
def dist_to(p): return min(cheb(a, p) for a in armies)                        # 取最近
near = [a for a in armies if 距离 ≤ 1]; movers[:need_n - len(near)]           # 成组 / 配额
```

候选 "军7→X" 的编码里只有**这一支**军队（`ag` 是 gather 一支），它不知道军3 更近。
另有：卷积只有 2 层 3×3 → **感受野 5×5**，两格以外的东西在候选的地块特征里根本不存在
（连"这格离我家多远"都算不出来）。

---

## 4. 外交现在就要占的位（回填时零改动）

| main 的状态 | 落到哪 | 现在 |
|---|---|---|
| `wars / truce / defense_pacts / guarantees / blocs` | **N 势力 token** 的"关系"字段 | 无他国 → 该组 mask 掉 |
| `proposals / peace_offers / votes` | N token 的"待议"位 + **E 事件 token** | 空 |
| `mailbox / mail_pending / gift_pending / map_pending` | **E 事件 token**（来信/馈赠/换图） | 空 |
| `maps / econ_intel / spy_pending` | N token 的"情报已知度" + E 事件 | 空 |
| `diplo_built`（外交中心数） | N token + G 全局预留位 | 0 |
| 20 类外交动作 | **`KIND` 预留槽**（现用 8 / 共 32） | 候选永不生成 |

> ⚠️ `KIND` 现在就定成 32。动作类型嵌入和子项嵌入的维度一变，ckpt 全废、
> embedding 白学。按路线图（会玩 → 外交 → 战争），这一步迟一天都是浪费。

---

## 5. 与 main 的一致性契约

**四条：**

1. **状态源唯一**：tokenizer 只读 `World` 的公开状态，不许有本分支私有的影子状态。
2. **信息集对齐**：能看到的东西 = main 里 LLM 玩家能看到的（`visible_to` + 自家面板）。
   **每个 token 组都必须能在 main 的面板里找到对应项**，找不到就是偷加了特权。
3. **动作集**：`KIND` = main 物理工具集（去掉只读 `query/report/rules/econ/countries`
   与 LLM 专属 `plan`）∪ 外交预留槽。
4. **枚举冻结**：见 `rl/vocab.py`。

**三条测试：**

| 测试 | 守什么 |
|---|---|
| `tests/test_vocab_main_parity.py` | 枚举表逐字逐序 = main；**RL 观测子表 = 冻结表 − MAIN_ONLY**；main 的工具集被 KIND 恰好覆盖 |
| `tests/test_branch_behavior_parity.py` | 引擎文件与 main **逐字节相同**；同一段动作流（v9 录、两引擎回放）下逐回合逐字段相同 |
| （待写）`test_tokenizer.py` | 同状态两次 tokenize 逐位相等；token 数不超预算；纯函数 |

**★2026-09-12 已处理：G 组的累计消费停供（契约第 2 条）**

契约第 2 条要求"每个 token 组都必须能在 main 的面板里找到对应项"。核查结果：

| | LLM 玩家看得到累计消费吗 |
|---|---|
| `main` | **看不到** —— `mp_ai.py` 的 Observer 面板是「国库/储备/国土/军队/关系/通信」，全仓搜 `累计\|spend_total`，`main:mp_ai.py` **零命中** |
| `feat/rl` | **看得到** —— `mp_ai.py` 第 174 行（Observer 面板）、第 619 行（每回合提示） |

而 `rl/env.py:568-570` 把 `w.spend` 的 build/recruit/supply 喂进了 G 组 ——
**那就是 `spend_total` 拆成三份**（`_spend()` 的 docstring：「全期累计，不随报表清零」，
`spend_total = sum(这三项)`）。所以 G 组里确实是 reward 的原函数，且是**唯一
在 main 面板里找不到对应项的 token 组**。

**处理（方向 B：从 RL 侧去掉，保留形状）**：
- `env.glob_channels()` 新增 —— 逐位通道名，取维**按名字不按下标**
  （写死下标是本仓库反复出事的模式）。`_obs()` 里有断言盯着名字表与构建顺序对齐。
- `tokenize()` 把 `spend:*` 三栏**置零**、宽度照旧。权重留着 → 那几维变成死权重，
  旧 ckpt 不废、将来要加回来也不用改形状。
- 有测试盯着（`tests/test_tokenize.py::TestNoScoreInObservation`）。

> 未处理的一条（**不是 RL 轨的事**）：`feat/rl` 的 LLM 面板**多**了这一行，
> 而 `main` 没有。论证文档 §5.4 主张"给 LLM 也加仪表盘"，但它自己在
> `指标质疑与回应.md` §4 承认**"尚未验证"**，并提了对照实验（同模型同 seed，
> 显示/不显示）。要不要动 LLM 轨，是那边的决定。

**已知的观测层特权缺口**（与 main 无关，是老师/学生之间的）：

- ✅ **2026-09-11 已修：老师不再越权看。** `expand_rule_v9.py` 加了视野门控
  （`vis()` = `World.visible_to`），堵掉四处：`tile_info()` 的 `_new_tile` 开图偷看、
  目标/落点对迷雾格算 `owned_by`/`tile_terrain`、兜底目标取全图野人列表。
  口径 = **LLM 玩家面板的信息集**（`threats` 面板就是 `visible_to`；`land` 面板只给
  野地地形字符、不给资源）。其余规则 AI（v3/v4/v5/v6）**尚未加门控**。
- ~~⬜ `world.tiles` 的持久性 vs 观测只给当前视野~~ → **2026-09-12 核实：这条不成立，已撤回。**

  原文说"学生不会记住走过的地方，**老师却记得**"——**老师不记得**。
  `expand_rule_v9.py` 读 `world.tiles` 只有四处（`tiles()` / `cnt()` / `slots()` /
  建造与征兵循环），**`p` 全部来自 `own`（自家地块，永远可见）**；
  目标地块走 `tile_info()`（内含 `visible_to` 门控），野人走 `vis()` 过滤。
  **老师的信息集 = 当前可见，与学生完全相同。**

  → **对 BC/P4 而言「记忆」无事可做**：老师不用它，就没有可学的东西，也没有
  不对称要补。原文称它为"「记忆」最硬的理由"——**那条理由没了**。

  记忆仍可能是**PPO 阶段**的事（智能体自己探索时，记住走过的地形有没有用），
  但那是**能力问题**，得单独论证，**不是"补上学生与老师的差距"**。
  记忆的定稿见 **§9**（2026-09-12 用户定：只限外交/事件/地图三类）。

---

## 6. 预算与成本

**训练机 = ECS（阿里云，1 物理核 / 3.3 GiB 可用）**，基准见 `训练机器性能简报.md`。
（原锚点「Windows 上一轮 PPO 8 局 10~15 分钟」随该机退出训练而作废；
按 Pi→ECS 的 fp32 训练比 5.9× 重锚。）

窗口 512 token、`d_model=192`、4 层 4 头、≈3M 参数：

| 项 | 值 |
|---|---|
| 窗口前向 | 参数 3.1 + 注意力 0.8 ≈ **3.9 GFLOP** |
| 候选 cross-attn | ≈ 0.74 GFLOP |
| 相对现状（0.13M 点积头，0.11 GFLOP/步） | **≈ 42×** |
| 一局 PPO 更新（现状 ≈ 1 分钟 @ECS） | ~~≈ 42 分钟~~ **实测 ≈ 8.9 分钟**（见下） |

> ★**2026-09-12 实测修正：上面那行"42 分钟"是拿 FLOP 比外推的，错了 7 倍。**
> 真实一局 BC @ECS = **531 s（8.9 min）**：19 s 采样+命中率 + 250 步 × **2.05 s**。
> 错在哪：122k 老模型跑 **batch 256**、候选补到批内最大（K≈400），**算力大半花在
> padding 上**；Transformer 跑 **batch 32**、GEMM 更大更高效。**FLOP 比 ≠ 秒数比。**
> 另：d192/4层实际 **2,083,986** 参数（不是本表估的 3M）。
>
> **真正的约束是内存**：窗口激活 ∝ `batch × token × d_model`，batch=256 时
> **自注意力掩码** `(B,H,T,T)` 一项就 422 MB —— 4 层合计把 ECS 压进 swap，
> 表现是"一步几分钟"，**看着像算力不够，其实是换页**。所以 batch 默认按主干定
> （tf 用 32），并打印粗估。

对照：3.4k 窗口 = 2,300×；16k = 26,000×。
**PPO 的更新侧是硬约束**——它必须对每个存下的 step 用新权重重算整窗前向，
KV cache 在这里无效。所以窗口大小由它定，不由采样定。

**★ 内存约束（ECS 3.3 GiB，硬）**：rollout 缓冲里**存帧、不存 token**。
512 token × d192 存 fp16 = 196 KB/步 × 1.2 万步 = **2.4 GB，装不下**；
存原始帧（bbox 网格 + 候选特征，~60 KB/步 ≈ 720 MB）则可行，
更新时**重建 token 窗口**。这条本来只是"更省"，在 ECS 上是"唯一可行"。

**★ bf16 / 线程数都要在真实模型上量过再定**：简报推荐 ECS 开 bf16，
但它自己那张表里 bf16（48,617 tok/s）**慢于** fp32（59,890），因为测的是 8.6 万参数的
玩具；我们的 3M + 512 序列矩阵大得多，bf16 大概率反超且省一半激活内存。
线程数同理（简报第 3 条：小模型多线程更慢）。别外推，量。

---

## 7. 落地顺序

1. `rl/vocab.py` + `test_vocab_main_parity` ✅ + `test_branch_behavior_parity` ✅
   （不动模型，先把"和 main 一致"钉死）
2. `rl/tokenize.py`：只出 token 张量 + mask，配纯函数级测试
3. 先接**现有点积头**跑通（tokenizer → 池化 → 原来的 q·c），验证 BC 命中不掉
4. 再换 Transformer 主干 + 候选 cross-attention，加记忆
5. 最后才开 PPO

第 3 步是故意的：**tokenizer 与主干分开验证**。否则换完分数掉了，
分不清是 tokenize 错了还是训练没上来。

---

## 8. 尚未决定 / 待办

- **记忆怎么做** —— ✅ **2026-09-12 用户定稿，见 §9**。
  口径：**只有三类**（外交记忆 / 事件记忆 / 地图记忆），机制是**定长槽位 + 年龄**，
  不是 RNN（用户判「短滑窗意义不大」「RNN 记不住空间」），也不是全上下文
  （§9 与 §6 给了 token 数上的硬理由）。
  ~~GRU/SSM vs 固定窗口~~ 这个取舍**作废**：不引入循环状态，
  记忆一律是「agent 侧缓冲 → 每回合刷新 → tokenize 读出」，与 minibatch 天然对齐。
- **K 关键节点组**要不要设（现在倾向不设，靠 map patch + 候选 cross-attn）。
- **E 事件 token 第 1 阶段要不要启用** `battle / tile_captured`（打野人也会产生）。
- ✅ **老师视野门控已做**（2026-09-11，`expand_rule_v9.py`）。v3/v4/v5/v6 未做，
  但它们已经不是基线/老师了。
- 地图尺寸：本设计对尺寸不敏感（patch 自适应），但**训练/评估目前仍固定 16×16**
  （`train.py` / `compare.py` 都没接 `--map-sizes`，只有 `bc.py` 有）。
  分布外测试要先把这条补上。

---

## 9. 记忆（2026-09-12 用户定稿）

用户口径原话：

> 要有记忆，不过**只限外交记忆，事件记忆和地图记忆**，同时**地图记忆可能需要的东西更多**，
> 包含**曾经占有的地块，视野内曾发现敌军，盲行军得到的报错信息**。

### 9.0 地基：记忆是 **agent 侧的**，不是世界侧的

这一条同时解释了「为什么它不违反契约 §1（状态源唯一）」，以及「为什么它必须这么做」：

| | 记忆存在哪 |
|---|---|
| LLM 玩家 | **它的上下文里**：`mp_ai.py:1321` 把工具返回值原样 append 进 messages；`_store_turn_memory`（`mp_ai.py:1040-1053`）**整回合完整存**，滑出后再由 LLM 压成 ≤800 字摘要（`mp_ai.py:1087`） |
| RL agent | `rl/memory.py` 里一个**agent 侧缓冲** —— 不改 `World` 一个字节 |

契约 §1 说的「不许有本分支私有的影子状态」指的是**世界状态**（不许自己维护第二份 `tiles`）。
记忆不是世界状态 —— **世界没有"玩家记得什么"这回事**，记得什么的是玩家。

**写入规则只有一条**：

> **记忆只能由「agent 当回合合法观测到的东西」写入。**
> 合法观测 = `env._obs()` 给的东西 ∪ 该回合动作的返回文本（`info["msg"]`；
> LLM 从工具返回值拿到的是同一句话）。

这条规则是**可测的**：每个记忆条目都必须能追到「第几回合、哪次观测 / 哪次动作」。
也正因为它可以测，它才挡得住"记忆悄悄变成偷看"——`_place_crosses`
（`mp.py:409-415`）把各国开局地块都建进 `world.tiles` 那件事就是这么被堵住的。

### 9.1 三类记忆各自的信息集锚点（每一类都能在 main 里找到对应项 ← 契约 §2）

| 记忆 | 引擎里已有的对应实现 | 现状 |
|---|---|---|
| **事件记忆** | `World.events_for(name, limit)`（`mp.py:366-378`）—— 已按 `visible_to` 门控：`h["nation"]==name` 无条件给，其余看**落点是否可见**。LLM 的【近讯】面板就是它（`mp_ai.py:379`→`_fmt_news`，limit=10） | **RL 侧一个字都没用**（E 组恒 mask） |
| **外交记忆** | `mp_ai` 的 Observer 面板的各国**国土/军队/关系**（`main:mp_ai.py:355`、`feat/rl:mp_ai.py:173`） | G 组已有**现值**（`rival_tiles/armies/hp`，`rl/env.py:619-625`）；**记忆补的是"上回合是多少"** |
| **地图记忆** | `World.visible_to`（`mp.py:334-348`）的**逐格历史** | 完全没有 |

### 9.2 ★先读这一条：独局下地图记忆的信息增量 ≈ 0（已实测）

**训练制式是单国独局**：`ZhanguoEnv` 的 `rivals` 默认 `()`（`rl/env.py:111`），
`bc.py` / `train.py` / `compare.py` **没有一个传 rivals**。所以地图上只有你 + 野人。

而 `_vision_mask`（`rl/env.py:412-435`）= 自家格 ∪ 八邻 ∪ 瞭望塔圆，
**自家领土在独局里只增不减**（没有对手能夺地；野人只防守不进攻，`mp.py:428-448`），
于是**视野是单调递增的**：今天看得见的 ⊇ 曾经看得见的。
野人还是**静态**的（`mp.py:431-433` 注释原话「野人是地图的静态属性」），
瞭望塔也不会被拆 —— 没有任何一条路径能让一个格子"看过又看不见"。

实测（v9 跑满 70 回合，`map ∈ {16,32}` × `seed ∈ {3,11}`）：

| map | seed | 曾见过 | 当前可见 | **曾见但当前不可见** |
|---|---|---|---|---|
| 16 | 3 | 55 | 57 | **0** |
| 16 | 11 | 52 | 55 | **0** |
| 32 | 3 | 83 | 85 | **0** |
| 32 | 11 | 81 | 81 | **0** |

→ **「把网格裁到探明外接框」在独局里与现在的可见外接框完全等价**（探明 bbox 11×11 vs
可见 bbox 11×12，逐格算过，没有一个"见过但现在看不见"的格）。
PLAN 里那条「很多地方要改」的担心（网格变大 → mlp 路 CNN 成本涨）**在独局里不存在**。

**它什么时候才有内容**：等**加对手**（`rivals≠()`）。那时领土会易主、他国军队会进出视野、
你在边境外看见过的东西会被忘掉 —— 这正是它的设计目标（路线图：会玩 → 外交 → 战争）。
**在那之前它是死权重**，和 G 组停供的累计消费三栏一样。

**独局下仍然真实存在、且只有一块**：**盲行军的遭遇**。因为

- 军队**不提供视野**（`visible_to` 只看领土与瞭望塔，`mp.py:334-348`）——
  这是"盲行军"的成因，不是 bug；
- 孤军在外时它脚下那格在观测里 `vis=0`、地形全 0（`_obs` 的 `chans[i] *= vis`，`rl/env.py:510-513`）；
- 你对那格发起 `atk`，引擎回的文本（`mp.py:807-819`："与野人交战" / "敌人为 0，拓疆「地块名」地形"）
  **只存在于动作返回值里**，观测里一个字都没有 —— 而 LLM 从工具返回值拿到了它。

最典型的一例：**同归于尽**（`mp.py:1010` 那一支）—— 你的军队和守军一起死光，
野地维持无主。观测里你只知道"军队没了"，**不知道那格现在空了**，
而下一回合你本可以零代价走进去。这条信息现在**两边都只存在于动作返回值里**：
LLM 留在上下文，RL 直接丢（`rl/env.py:183` 的 `info["msg"]` 谁都没接）。

### 9.3 地图记忆：逐格「最后已知 + 我的遭遇」

**新增 `rl/memory.py`**（agent 侧，随 episode 重建，不被 `World` 引用）：

```python
tile[(x, y)] = {
    "seen_turn": int,          # 最后一次看见它的回合；-1 = 从没看见过
    "terrain": str | None, "resources": dict,
    "owner": str | None, "buildings": dict,
    "probe": None | {"turn": int, "kind": ProbeKind, "note": str},
}
foes[key] = {"owner", "kind", "x", "y", "hp", "seen_turn"}
```

- **`probe`（★盲行军得到的报错信息）** —— `kind` 是个小冻结表：
  `atk_lost`（同归于尽/打输，那格现在是空的/仍有守军）、`blocked`（不可进入）、
  `atk_ok`（打赢了/无守军，进驻）。
  来源就是 `_apply` 的返回值 `(ok, msg)`。
- **写入时机只有一处**：每回合末（`_run_round_end` 之后）按该回合的 `_vision_mask()`
  刷新 `tile`，并把本回合拿到的带落点动作结果写进 `probe`；把当前可见敌军刷进 `foes`。
  **别分散写** —— 分散写就没人能证明"每条都来自合法观测"。

**读出（`tokenize`）**：

- **网格**：bbox 从「可见外接框」扩到「`tile` 里出现过的格的外接框」。独局下二者相同（§9.2），
  加对手后才有差别。非当前可见但见过的格**填最后已知值**；新增两个通道：
  `remembered`（1 = 这格我有记忆；与 `visible` 正交，解决"零值和没记忆分不清"）、
  `probe`（1 = 我在这格碰过壁）。→ **通道数 36 → 38**（一次加完，之后不再动）。
- **A 组**：自家（原样，**必须在前的顺序不变** —— `army_idx` 指着它）
  + 当前可见敌军（原样）+ **曾见但当前不可见的敌军**
  （按 `(age, owner, x, y)` 确定性排序，接在可见段之后）。
  → `F_A` 第 12 维**"情报年龄"终于能填**（`age = turn - seen_turn`，当前可见的填 0）。
  `rl/tokenize.py` 的「偏离说明 2」就是为它留的位。
  **截断规则**：超 192 时从最旧丢，丢了多少记进 `meta`。
- **K 组**（16 槽，原本"关键节点"、倾向不设）**改作地图记忆的稀疏槽**：
  值得单独钉住的格（碰过壁的、曾属于我的、远处见过资源的），带 `(dx,dy)` + 类型 + 年龄。
  **为什么需要它**：M 组恒 ≤64 patch、块边长随外接框自适应 ——
  外接框一大，**丢的恰恰是"哪一格特殊"**（`rl/tokenize.py` docstring 已记这条代价）。

### 9.4 事件记忆（E 组，32 槽）

**两个来源，都是 LLM 上下文里本来就有的东西**：

1. **引擎日志**，口径直接用 `World.events_for`（`mp.py:366-378`）—— **别自己写第二份过滤**
   （契约 §1）。它只读、不碰 `history_seen`，可以放心调。
2. **自己的动作与结果**（`info["msg"]`）—— `rl/env.py` **现在完全不记**，
   也**不写 `world.history`**（与 `mp_ai.py:816` 的 `log_tool` 不同）。所以这块现在是纯丢弃。

**★容量问题（先解决它）**：PPO 下一回合 ~70 步，32 槽连本回合都装不下；
而**本回合的动作本来就还在状态里**（`turn_actions`、国库、地块…），逐条记它没有意义。
所以 E 组**按回合装填**：

- 每次 `end_turn` 时，把本回合的动作结果**压成 ≤2 条摘要**入池
  （"本回合：3 次进攻，占 2 格，1 支阵亡"），**逐条不入池**；
- 引擎日志（`events_for` 口径）**按条**入池；
- 池满丢最旧，`age = turn - 事件回合`。

**这是对原 R 组（"每回合池化 2 个 × 近 4 回合"）的收编**：机制留着，但**只服务事件记忆**。
→ **R 组退役**（恒 mask、全零）：用户口径只有三类记忆，第 4 类"通用记忆"没有归属。

**宽度 `F_E = 12`**：类型 4（one-hot：行动 / 战报 / 领土 / 内政）+ 年龄 1 + actor 槽 2
+ 落点 `(dx,dy)` 2 + 有落点 1 + 量级 1 + ok 1 = 12。
（`tokenize.py` 里 `F_E` 现值 12 —— **宽度不用改**，只把语义定死。
actor 槽沿用 `NATION_SLOTS` 的槽位口径，野人单占一槽。）

### 9.5 外交记忆（N 组，8 槽）

**单国独局下无外交**：`rivals=()` → 一个对手都没有（引擎只建 `["秦"]`）。
所以 N 组**现在恒空**，与 `tests/test_tokenize.py::TestDiploReserved` 的断言一致 ——
**这一条不用改**。（引擎那侧的外交代码在，但独局里没有对象可用。）

定死的是**加对手 / 外交回来时填什么**：每国一个 token ——
关系 6 one-hot + **领土现值/增量** + **军队现值/增量** + **HP 现值/增量** + 接壤
+ **上次观察回合** + 外交中心数 + 有无待议。

**现值来自面板、增量来自记忆**：LLM 的上下文里有它每回合读过的面板，所以它算得出增量；
RL 若只有现值就算不出。**这是这条记忆补的唯一一处不对称** —— 也正因如此，
它和 G 组的 `rival_*` 现值**不重复**（现值照旧在 G，N 只放"变化"）。

### 9.6 落地顺序（含一条必须由用户拍板的建议）

| 步 | 内容 | **独局下有没有信息增量** |
|---|---|---|
| 0 | **口径先一次性定死**：通道 36→38、`F_A` 第 12 维启用、`F_E=12` 语义、`F_N` 加增量位 | — |
| 1 | **事件记忆**（E 组回填 + 回合摘要；接 `events_for` 与 `info["msg"]`） | **有**（引擎日志 + 动作结果**现在全丢**） |
| 2 | **地图记忆**（`rl/memory.py` + `remembered/probe` 通道 + A 组情报年龄 + K 组） | 只有「碰壁遭遇」一块 |
| 3 | **外交记忆**（N 组） | **无**（本分支没对手） |

**★建议（需用户确认，因为它决定先做什么）**：2 和 3 的真正内容都**依赖"有对手"**。
- 短期仍是独局 → **1 是唯一有增量的**，先做 1；2/3 留到加对手时一起落地，
  否则训出来的记忆权重是**死权重**（G 组停供的累计消费三栏就是先例）。
- 短期要**加对手**（多国对抗/自对弈）→ 2 和 3 **必须与加对手同时落地**，否则白跑一炉。

> ✅ **2026-09-12 用户拍板：「基本会玩了再说」。**
> → **§9 整节顺延**：`rl/memory.py`、`remembered/probe` 两通道、`F_A` 第 12 维、
> K 组改作稀疏槽 —— **现在一个都不动**。先把「会玩」跑出来（P4 评估 → PPO），
> 等基本会玩了再回来做记忆（那时多半也正好要加对手，2/3 一并落地）。
>
> **这条顺延的附带好处是实打实的**：§9 一动就改观测 ⇒ 正在跑的 BC 那炉 ckpt 全废
> （§9.6 步 0 也是观测改动）。**不动，就等于保住这炉**，可以直接接着往下走。
> 将来动手时的顺序不变（步 0 → 1 → 2 → 3），只是起点推迟。

**⚠ 重炼铁律照旧**（PLAN §三）：任何一步都改观测 → 在跑 / 已跑的 ckpt 全废。

### 9.7 ★盲行军是**探测手段**，不是 bug（用户 2026-09-12 追加口径）

> 原话：「迷雾泄露很有价值，因为**盲行军可以探索敌国位置**」。

**口径定死**：`move`/`attack` 被拒时那句「不可入境：(x,y) 是「楚」的领土」**不是要修的洞，
是游戏给的探测传感器**。玩家花一步（军队本回合的移动额度）换一条情报，这是**代价换信息**的
正常设计，与"偷看"的区别恰恰在于**你要付出动作**。所以：

- **不堵**。不改成"前方情况不明"之类的模糊回话 —— 那是**换规则**（用户口径：别换规则）。
- **要记**。拒绝消息落进 §9.3 的 `tile[(x,y)]["probe"]`（`kind="blocked"`，记下 `owner`）。
  于是「曾经占有的地块 / 曾见敌军 / 盲行军报错」三件事在**同一个结构**里闭环。

**★★ 但有个前提必须先拆掉：RL 的候选枚举现在用真值过滤，等于把这份情报白送。**

`rl/env.py` 有三处拿 `World` 的**真值**做筛选，Agent 从"**哪个候选不在表里**"就能反推出
情报 —— 不用花动作、不用探测，而且**一次覆盖整个速度半径**，比那句拒绝消息**强得多**：

| 位置 | 现状 | 后果 |
|---|---|---|
| `rl/env.py:337-341` | `o = w.owned_by(x,y)`；`if o is not None and o != me: continue` | **速度半径内少了哪个候选 = 那格是他国领土**（"不可入境"的信息白拿） |
| `rl/env.py:342-343` | `busy = any(d["owner"] not in ("野人", me) and d.get("engaged") and ...)` 扫**全图** `w.armies` | 候选被压掉 = 那格有看不见的他国军队在打野 |
| `rl/env.py:232-234` | `_retreat_legal` 用 `owned_by` 真值判八邻 | 撤退候选的形状泄露四周 8 格的归属 |

**改法：把这三处的判据从「世界真值」换成「agent 的记忆」**（§9.3 的 `tile`）：

- 记忆里**已知是他国** → 不生成（探测过了，别重复付代价）；
- 记忆里**不知道** → **照生成**，让它去撞、去被拒、把结果记回来。

这样就形成一个干净的自限循环：**探测一次 → 记住 → 候选里消失 → 不会再付第二次代价。**
（独局下这三处本来就一次都不触发 —— `rivals=()` 时 `owned_by` 只可能返回自己或 `None`，
所以这条改动**在独局里是 no-op**，可以安心先落代码、等加对手自动生效。）

**⚠ 但要说清 PPO 会不会自己发现它**：**RL 的动作空间与观测里没有外交/战争**
（引擎里有，见 `mp.py:8-12`；但 `rl/env.py` 的 `KINDS` 只有 8 项、观测子表也剔掉了外交项），
而 reward = **自己的总消费** —— 知道楚在哪，**当前训练线里没有任何可兑换的收益**。所以：

- 独局 + 无外交 ⇒ **PPO 学不出探测**（探测花一步、收益 0），别指望；
- 战争/外交回来之后 ⇒ 探测才有价（找到敌国城池去打），那时 PPO 才可能自己发现它。

→ 所以 9.7 的正确落地时机和 §9.6 的 2/3 步**完全一致：跟着加对手 / 外交一起上**。
**在那之前，它是一条"设计上留着、行为上禁用"的规则** —— 写在这里是为了
**别在将来看到"候选里有注定失败的动作"时把它当 bug 删掉**。

---

## 10. 下一版观测改动清单（2026-09-12 **口径已拍板**，实施前必读）

用户当日两句原话（这份清单就是为它们写的）：

> 「等这个炼好了，下次炼的时候多留点位置，炼一个更通用的模型，更宽泛的 4x 模型。」
> 「这炉模型只能是版本特化的，如果我平衡调整，例如石油发电厂降价，他就拟合不上了。」

**为什么必须一次做完**：改动让 ckpt 失效就必须重炼（PLAN §三）。下面三件事各自都会改
观测/训练分布，所以要搭同一班车，别各开一炉。

| 想要的能力 | 靠什么 | **不**靠什么 |
|---|---|---|
| 改数值（石油能源厂 240→180）不必重炼 | ① 数值进输入 + ② 训练期域随机化 | ③ 留位 |
| 加实体（新建筑/新兵种）不炸形状 | ③ 词表留位 | ①② |
| 「更宽泛的 4X」也能用 | ①②③ **缺一不可** | 任何一项单独 |

### 10.1 基线（实测值；改完逐项对照，任何一项对不上就是漏改）

**网格通道 36**（`env.obs_channels()`，`rivals=()`）：
`terrain 0-4` / `res 5-9` / `owner 10-11`（`1+rivals+1`，**动态**）/
**`bld 12-26`（15 条 = `vocab.BUILDING` − `MAIN_ONLY`）** /
标量 27-35（`mine/frontier/my_army_hp/foe_army_hp/barb_army_hp/pending/built_this_turn/visible/home`）

**全局向量 48**：`res 7 / price 6 / eq 6 / spend 3 / 标量 5 / bld 15 / armies 2 / army_kind 3 / own_tiles 1`

**token**：`CAP = {g:1, m:64, a:192, n:8, e:32, r:8, k:16}` 合计 **321**（预算 `TOKEN_BUDGET=512`，余 191）。
宽度：`g = glob_size()+8 = 56`（+8 是外交预留）、`m = 通道数+3 = 39`、`a = F_A = 12`、
`n/e/r/k = 16/12/16/8`。M 组那 3 维 = **patch 中心相对家 dx、dy（÷`POS_SCALE`）+ 块内可见占比**。

**候选字段**：`type_idx`(0-7) / `sub_idx`（按 kind 查 `env.sub_tables`）/ `tile_dx,dy`（−1=无落点）/
`army_idx`（null 槽 = 军队数）/ `amount_idx`(0-9)。

**两套头**：P3 `PolicyNet`（CNN 吃 grid + 点积头；价值头吃 `glob`）、
P4 `WindowTransformer`（**不吃 grid**，cross-attn 打分；价值头吃窗口掩码均值池化）。

**★ 决定改哪两处的现状事实**：`sub_idx` 在两边都是**纯查表** `nn.Embedding`
（`model.py:81,129-131`；`transformer.py:181-183`）；候选里唯一带引擎内容的是 `army_feats`
（兵种 one-hot / hp / 坐标，`env.py:584-592`）；**建筑的造价·产出·耗电·燃料·门槛一个字段都没进模型**。

### 10.2 改动一：规则表数值进输入

**进什么**（三张表，逐字段；`game.py:134-173` / `176-183` / `95-122`）：

| 表 | 字段 | 去向 |
|---|---|---|
| `BUILDINGS` | `kind`(11 种) / `cost` / `wood` / `cap_resource` / `outputs` / `inputs` / `fuel` / `energy_out` / `energy` / `limit` / `min_slots` / `max_level` | 新 token 组 **`b`** + 候选内容嵌入 |
| `UNIT_TYPES` | `hp` / `speed` / `supply` / `atk` / `recruit`(7 物资) | 新 token 组 **`u`** + 候选内容嵌入 |
| `MARKET` / `TRADEABLE` | 基准价 / `MARKET_DEPTH` / `MARKET_SPREAD` | v1 先只把**基准价**并进 `u`/`b` 的物资向量；单独 `x` 组留待需要时 |

**建筑内容向量 `F_B = 45` 维**（要改就得连同 10.6 的口径表一起改，别单独动）：
`kind one-hot 11` + `cost/2000` + `wood/100` + `cap_resource one-hot 6`（5 种资源 + "无"）+
`outputs 7` + `inputs 7` + `fuel 7`（按 `STOCK` 顺序，除以各自的常见量级）+
`energy_out/8`、`energy/4`、`limit/8`、`min_slots/20`、`max_level/5`。
**归一化尺度写死在这张表里**，不许实现时"看着办"——随机化和训练分布都依赖它。

**载体 A：新增 token 组 `b`（建筑）与 `u`（兵种）**（窗口侧，给价值头与注意力用）
`CAP["b"] = 20`（16 活跃 + 4 留位，见 10.3），每条 token = `[我的持有数 log1p/3, 内容向量 45]` → 宽 **46**；
`CAP["u"] = 5`（3 活跃 + 2 留位），每条 = `[hp/200, speed/2, supply/2, atk/100, recruit 7 物资 /10]` → 宽 11。
空槽 `mask=0`（不参加注意力，也不占语义），新建筑上线时把对应槽 `mask=1`。
接入点：`tokenize.py:62-76`（`GROUPS`/`CAP`/宽度常量）、`:261-279`（`feats`/`mask` 字典）；
`ppo.py:182-211`（`collate_window` 按 `GROUPS` 自动展开）；`transformer.py:97/101`（自动展开）。

**载体 B：候选嵌入带内容**（策略侧，让"该不该建它"能按数值算）
`se = content_proj(内容向量) + sub_emb(sub_idx)` —— 学到的残差 + 规则表内容。
落点：`model.py:129-131`、`transformer.py:181-183`；**拼进 `se` 后必须同步改**
`model.py:134` 的 `cat` 与 `:83` 的 `d_in`、`transformer.py:195` 与 `:116` 的 `d_q`
（维度错会当场报错，不会静默）。

**为什么两处都要**：候选侧给**策略**（"现在建它划不划算"），窗口侧给**价值头**（"这局面和这组
数值下，局势值多少"）。只做候选侧，价值函数仍然看不见数值，PPO 的优势估计照样错。

### 10.3 改动二：词表留位

**现状盘点**（哪些表已有真空位）：

| 表 | 现值 | 空位 | 消费点 |
|---|---|---|---|
| `vocab.BUILDING` | 16（含 `MAIN_ONLY` 的外交中心，**位置被占不是空位**） | **0** | `env.bnames`（15） |
| `vocab.UNIT` | 3 | **0** | `env.unames` |
| `vocab.TRADEABLE` | 6 | **0** | `env.goods` |
| `vocab.KIND` | 32 | 4 个 `_reserved_*` ✓ | 仅测试 |
| `vocab.NATION_SLOTS` | 8（实测 5 国） | 3 ✓ | `CAP["n"]` |

**留位方案（✅ 用户 2026-09-12 拍板：「少留」+「其他也留位」）**：

| 表 | 现值 | 留后 | 加 |
|---|---|---|---|
| `BUILDING` | 16 | **20** | +4（最可能长的一类，多给两个） |
| `UNIT` | 3 | **5** | +2 |
| `TRADEABLE` | 6 | **8** | +2 |

留位项一律：**内容全零 + 独立 id + 永不生成候选 + 只在 `b`/`u`/… 组里占槽（`mask=0`）**。

**成本（要认，这是这版的"定金"）**：

| | 现在 | 改后 | 为什么 |
|---|---|---|---|
| 网格通道 | 36 | **42** | +4 建筑留位 +2 §9（10.5） |
| `m` 宽度 | 39 | **45** | = 网格 42 + 3 |
| glob：`bld` 段 | 15 | **19** | +4 |
| glob：`price`/`eq` | 各 6 | 各 **8** | +2 商品 |
| glob：`army_kind` | 3 | **5** | +2 兵种 |
| glob 合计 | 48 | **58** | 上面四项 |
| `g` 宽度 | 56 | **66** | = glob 58 + 8 外交预留 |
| `F_A`（A 组宽） | 12 | **14** | 兵种 one-hot 3→5 |
| `ARMY_FEAT` | 8 | **10** | `env.py:46` 模块级常量 |
| token 合计 | 321 | **346** | +`b`(20) +`u`(5)，预算 512 ✓ |

（各留 1 个也能省：`F_A` 13、glob 55 —— 差 3 维，不值得为此少留一个。所以按"每类留 2"写。）

**⚠ 连锁（不先想清楚，改完会红一片）**：
1. **「vocab 表 == main 表」不再成立** —— 三张表都是「活跃段 + 留位段」。`test_vocab_main_parity`
   里的 `test_building_is_main_16` / `test_building_includes_diplomacy_one` /
   `test_terrain_unit_tradeable_match_main` / `test_local_buildings_equal_frozen` 要改成
   **「main 的表是 vocab 的活跃前缀」**（`vocab.BUILDING[:16] == main 的 16`，UNIT/TRADEABLE 同理）。
2. **`tokenize.py:243` 有一条 `assert len(UNIT_KINDS3) == 3`** —— 兵种一留位就会炸，要改成
   `== len(UNIT)`（或直接删，改由宽度断言守）。
3. **`F_A` 的算式**（`tokenize.py:72` `3+3+1+2+1+1+1`）里那个写死的 `3` 要改成 `len(UNIT)`
   —— 否则兵种一多，one-hot 位数与 `_army_row` 的写入顺序对不上（**这是静默错位型**，最危险）。
4. `env.py:46` `ARMY_FEAT = len(UNIT_TYPES)+1+2+1+1` 是**模块级常量**，改了会连带
   `model.py:15,78`、`transformer.py:49,114` 的 `nn.Linear` 宽度 —— 这是**显式报错**，安全。

**⚠ 顺手处理一个旧坑**：`vocab.GRID_CHANNELS = 45`、`GROUP_CAP`、`PATCH_CHOICES`、`OWNER_CHANNELS`
都是**计划值且无人消费**，其中 45 与实测 36 口径不一致（45 按"8 国槽 + 16 建筑 + 8 标量"算的）。
本版要么**接线**（让它由实现推导并被断言），要么**删掉** —— 别再留两套口径等着踩。

### 10.4 改动三：训练期域随机化（**引擎文件一行不改**）

**可行性已核**：`mp.py:26-63` 是 `from game import BUILDINGS, UNIT_TYPES, MARKET, TERRAIN_STATS, TRADEABLE, ...`
—— **dict/list 拿到的是同一对象，就地改立即生效**（`BUILDINGS["石油能源厂"]["cost"] = 180` 就够了）。
标量常量（`MAX_SLOTS`/`ENGINEER_DISCOUNT`/`WATCHTOWER_RADIUS`…）是**值拷贝**，改 `game.X` 传不到 `mp`，
**v1 不碰这些**（要碰就得同时改 `mp` 命名空间，那是另一件事）。
全仓目前**没有任何地方**给这些表赋值 —— 域随机化会是首例，所以纪律要写死在代码里。

**钩子**：唯一的环境边界是 `ZhanguoEnv.reset(seed)`（`env.py:160-182`），**已有先例**：
`map_size` 就是按 `random.Random(seed ^ 0x9E3779B9)` 每局重采样的。规则表同样：
`rules_seed = random.Random(seed ^ 0x5221E)` → 重采样 → **就地写进 `game.*` 的 dict** → 记进 run 目录。
三个训练入口都走 `reset`：`bc.py:129/148`、`train.py:90/261/284`、`compare.py:27/58`，
不需要在调用方各加一处。

**v1 随机什么 / 不随机什么**：

| 随机 | 幅度起点 | 理由 |
|---|---|---|
| `BUILDINGS[*].cost` / `wood` | ±20%（对数均匀） | 就是用户举的那个例子 |
| `BUILDINGS[*].outputs` / `inputs` / `fuel` / `energy_out` | ±20% | 产出侧 |
| `MARKET[*]` 基准价 | ±20% | 与价格同族，模型已在 condition 价格 |
| `UNIT_TYPES[*].hp` / `atk` | ±20% | 兵种强弱 |
| **不碰** `cap_resource` / `min_slots` / `limit` / `max_level` | — | 它们改的是**可行性拓扑**（哪些格子能建），不是数值；一抖就换了游戏 |
| **不碰** `UNIT_TYPES[*].supply` | — | ★`spend_rules.py:26` 有一份**硬编码副本** `SUPPLY_PER_ARMY = {"步":1,"骑":2}`，老师 v9 走它；抖了会让"引擎结算"与"老师的账"不一致 |

**纪律**：
1. **同源**：候选过滤、引擎结算、观测特征（`b`/`u` 组从同一张活表取）、**老师**都必须读同一份
   ——否则模型学的是假动力学。★**老师必须用 `--teacher v10`**：v9 把引擎事实写死在常量里
   （每地形要几支兵、满血阈值、征兵成本），抖了之后它按错的值决策，给的标签就是错的。
   实测（20% 抖动 / 5 seed / 120 回合）：v9 在 **3 个 seed 上崩盘**（只占 5 格、0 次进攻），
   v10 照常打，终局消费 **+12.5%**；40% 时 +8.6%；不抖时 +2.1%。
   v10 的另两处：去掉"绕山地"（改看打不打得赢）、以及所有引擎数值现读，见
   `expand_rule_v10.py` 头部。
2. **可复现**：`seed → 表`；把 `rules_seed` 与抖动量写进 run 目录（对拍/回放的前提）。
3. **评估期关**：`compare.py` / `bench.py` / `train.py` 的评估一律**真值**；随机化只在训练采样期开。
4. **还原**：每局结束还原真表（或每局用 `try/finally` 包住），别让残留污染后续评估。
5. **调法**：`0 → 0.1 → 0.2` 三档看 BC 命中率与终局消费；幅度太大时策略会退化成"什么都不敢建"
   （回本不确定 ⇒ 保守），那时的信号是**终局消费中位数下降**，不是 loss 上升。
6. ★**幅度上界有实测**（用户 2026-09-12 追问「过大抖动会不会让老师启动不起来」）：
   20 张地图 × 80 回合、老师 v10：

   | 抖动 | 老师"启动不起来"的图 | 形态 |
   |---|---|---|
   | ±0% | 0/20 | — |
   | **±20%（当前用这个）** | **0/20**（20 图）／**3/50**（50 图，见下） | 健康局最少 12 格、8 次进攻 |
   | ±35% | 2/20 | 领地停在开局 5 格、0~1 次进攻 |
   | ±50% | 4~6/20 | 同上，另有 7 格/2~4 次进攻的"半启动" |

   ★**50 张图（seed 0~49、70 回合，与训练逐集对齐）的分辨**：3/50 被判退化，但延长到
   150 回合后分成两类 —— **慢启动** 2 张（seed 2/12：150 回合能到 79/83 格、96/98 次进攻、
   消费 11k/13k）与**真卡死** 1 张（seed 20：150 回合后仍是 5 格 / 0 兵 / 消费冻结 1,793）。
   ⇒ ±20% 的真实比例是 **1/50 真卡死（2%）+ 2/50 慢启动（4%）**。
   （离线复算与训练日志**逐集消费完全一致** —— 这套测量可以信。）
   慢启动那两局的样本不是错的，只是"相位"偏经济（70 回合全在建产能）；真卡死那局才是毒样本。

   ⇒ **±20% 在安全区内，35% 以上开始有地图玩不动**。判据是**引擎侧的领地数**，
   不是"老师这一局的消费"（退化局照样烧 3k~4k —— 卡住的帝国也在建东西、养兵）。
   `rl/bc.py` 的 `episode_is_degenerate` 就是这条的守卫：退化局的样本全是 end_turn，
   收进缓冲等于教学生"别动"，所以**整局丢弃**；回合数 < 40 时不判（扩张本来就晚）。

### 10.5 接口预留（记忆 / 加对手）：**只留位，不实现**

**§9 的 +2 通道并进本版（✅ 用户 2026-09-12 拍板）** —— 这正是 §9.3 原本写的
「通道数 36→38（**一次加完，之后不再动**）」。本版网格 = `36 + 4(建筑留位) + 2(§9) = 42`。

**但记忆的"填值"仍顺延**（用户「基本会玩了再说」）：本版这两条通道**恒零**，
`F_A` 的**最后一维**（"情报年龄"槽**已存在**、恒 0；留位后位置随 `F_A` 12→14 顺延，
§9 落地时只是开始填它，宽度不再动）、`F_E` 语义、
K 组稀疏槽、`rl/memory.py` —— **一行都不动**。将来 §9 落地时**宽度不再变**，只填值。

### 10.6 ★口径清单表（一次过完，逐项签字）

| # | 口径 | 决定 | 宽度影响 | 影响的测试 | 失效产物 |
|---|---|---|---|---|---|
| 1 | `b` 组：CAP 20、宽 46、空槽 mask=0 | ✅ | token +20 | `test_tokenize` 宽度/预算 | 全部 ckpt |
| 2 | `u` 组：CAP 5、宽 11、空槽 mask=0 | ✅ | token +5 | 同上 | 全部 ckpt |
| 3 | 候选 `se` 含内容向量（P3+P4 两处都做） | ✅ | 模型参数 | `test_window_head`/`test_transformer` | 全部 ckpt |
| 4 | 归一化尺度表（10.2 那张，写死） | ✅ | — | 新增：尺度断言 | 全部 ckpt |
| 5 | 留位：**建筑 16→20 / 兵种 3→5 / 商品 6→8** | ✅ | 网格 36→**42**、glob 48→**58**、m 39→**45**、g 56→**66**、F_A 12→**14**、ARMY_FEAT 8→**10** | `test_vocab_main_parity` 4 条重写成「main 是活跃前缀」、`test_rl_env` 36/48→42/58、`tokenize.py:243` 断言 | 全部 ckpt |
| 6 | `vocab.GRID_CHANNELS` 等计划值：接线或删 | ✅ | — | `test_vocab_main_parity` | — |
| 7 | 域随机化：字段表 + ±20% + **默认关**、训练期 `--rules-jitter` 开 | ✅ | — | 新增：可复现、关==真值、同源 | 全部 ckpt |
| 8 | §9 的 +2 通道并进本版（只占宽、恒零、不填值） | ✅ | 已并入第 5 行的 42 | — | — |
| **9** | **以上任意一项落地 ⇒ 所有既有 ckpt（含 ECS 上那炉）全部作废** | ✅ | — | — | **BC/PPO/P4 全废，必须重炼** |

### 10.7 落地顺序

| 步 | 做什么 | 验收 | 状态 |
|---|---|---|---|
| 0 | **本清单定死**（10.9 的岔路全部拍板） | 用户点头 | ✅ 2026-09-12 |
| 1 | 词表留位 + 内容向量表（`rl/vocab.py`、新 `rl/features.py`） | 新测试：内容向量与引擎表**同源**、留位项全零、尺度锁定 | ✅ `fd7bb6c` |
| 2 | 观测接线（`env.py` 网格/glob、`tokenize.py` 新组 `b`/`u`） | 形状测试：新宽度逐项对齐 10.1 表 | ✅ 同上 |
| 3 | 模型接线（两处 `se`、两个价值头路径） | `test_window_head` / `test_transformer` 绿；形状断言 | ✅ 同上 |
| 4 | 域随机化（`rl/jitter.py` + `env.reset` 钩子 + `--rules-jitter`） | 可复现测试、关==真值、老师与引擎同源 | ✅ 见下 |
| 5 | 单炉验证：BC 40 局（`--rules-jitter 0`） | 命中率/验证集不塌（与旧炉同 seed 可对照） | ⏳ 待 ECS |
| 6 | 带随机化重炼 + 与旧 ckpt 的对比协议 | 同一批图/同 seed 比终局消费中位 | ⏳ |

**实施记录（2026-09-12，与本文档的偏差只有两处，都记在这里）**：
1. `CAP["b"] = 19`（= `len(OBS_BUILDING)`，不含 MAIN_ONLY 的外交中心）而不是 20
   → token 合计 **345**（不是 346）。外交中心不进观测，自然也不该有 token。
2. 域随机化的**小整数**字段（`outputs`/`inputs`/`fuel`）不能照抄乘法：1×1.2 取整还是 1，
   等于没抖。改用「乘法取整 + 没变化时按幅度概率走一格（±1，下限 0）」——
   **保持整数**是硬要求（引擎里到处是 `//`、`<= 0`、`int()` 的整数口味假设，
   让产出变成小数会把库存变成浮点，那是另一件事）。见 `rl/jitter.py` 的 `_jitter_small_int`。
3. `energy`（工厂耗电）**没抖** —— §10.4 的表里没写它，本版严格按表执行。
   要抖就加进 `_BUILDING_SCALAR`，但它是小整数，得走 `_jitter_small_int`。

**实测的副作用（不是偏差，是设计的一部分）**：`env.reset()` 现在每次都会
`jitter.apply(seed, amount)`（amount=0 时等价 `restore()`），所以**任何** env 的 reset
都会把进程级的 `game.*` 表设成它那一局的口径。因此**评估 env 与训练 env 不能混用同一个进程**
（`train.py` 里已经分开：`eval_env = build_env(args, jitter=0.0)`）。

---

## 10.10 复杂建筑/地形/军队能不能表达？（用户 2026-09-12 追问，逐条审计）

用户原话：「例如工程院，市政厅，瞭望塔，军屯等复杂建筑能表达吗，至少市政厅和工程院应该
要能表达，地形，城堡属性 buff 也应该要能表达，军队也不只有 hp，atk，还有移动距离」。

### 结论先说：分三层，问题在第三层

| 层 | 内容 | 现状 |
|---|---|---|
| ① 已在数据里、已进输入 | 采集/能源/工厂/黄金矿场的 `outputs`/`inputs`/`fuel`/`energy`/`energy_out`/`cap_resource`；造价/木耗/上限/位次/等级；**兵种全部数值含 `speed`（移动距离）** | ✅ 10.2 已接 |
| ② 已在数据里、**还没进**输入 | **地形属性** `TERRAIN_STATS`（`defense` 减伤 / `build_penalty` 造价惩罚）；城堡的**逐级造价表**（现在只取了 L1） | ⚠️ 只是没接，好补 |
| ③ **效果根本不在数据里** | 工程院 −25%、市政厅产金、瞭望塔视野 4、城堡 +10% 防御/级、兵营与军屯的**征兵/民兵上限**、外交中心减免 | ★ **特征向量再宽也表达不了它**——它不在表里 |

### 第 ③ 层的证据（逐条查过代码，不是猜测）

| 建筑 | 它的 `BUILDINGS[name]` 里有什么 | 效果实际在哪 |
|---|---|---|
| 城堡 | `kind/cost(逐级表)/wood/max_level=5` | `mp.py:1061` `cd = castle * CASTLE_DEFENSE_PER_LEVEL` ← 全局常量 |
| 市政厅 | `kind/cost/wood/energy/limit/min_slots` | `TOWN_HALL_GOLD=5`、`TOWN_HALL_PER_SLOT=1` ← 全局常量 + mp.py 逻辑 |
| 瞭望塔 | `kind/cost/wood`（**零效果字段**） | `WATCHTOWER_RADIUS=4` ← 全局常量 |
| 工程院 | `kind/cost/wood/limit/min_slots` | `ENGINEER_DISCOUNT=25` ← 全局常量（且"只认已落成"在 mp.py 逻辑里） |
| 兵营 | `kind/cost/wood/energy/min_slots/army_cost` | 「每兵营 1 支/回合」**写死在 `mp.py:748`** |
| 军屯 | `kind/cost/wood/limit/outputs` | 「每军屯 1 支民兵/回合」**写死在 `mp.py:734`** |
| 外交中心 | `kind/cost/wood/limit/limit_nation/min_slots` | 减免规则在 `mp_ai.py` 的费用函数里 |

**所以现在的模型对这几座建筑，只能从"建了之后发生了什么"里反推**（回到"自己建模效果"，
即用户最初问的那个问题）。`features.py` 给的是"能表达的那部分"，表达不了这六座 ——
这一点必须说清楚，别让"已经喂了规则表"给人一种全都喂了的错觉。

### ✅ 已拍板并落地（2026-09-12）

用户拍板：**走 (B) 搬进 `game.BUILDINGS`**，并把四件"数据已在、只是没接"的一并补上
（地形属性、城堡逐级造价+防御、军队行 speed/atk、网格建造成本通道）。

**main 侧**（`b8ee3f8`，行为等价已核：同一段动作流 40 回合 196 次调用逐回合全同）：
六条效果搬进 `BUILDINGS[*]["effects"]`，消费方（mp.py / mp_ai.py / spend_rules.py /
settlement.py）全部改读表；新增 `game.building_effect(name, key, default=0)` 唯一取值口。
**顺带的收益**：效果成了 dict 里的数 → **域随机化能抖它们了**（以前是值拷贝，抖不动）。

**RL 侧**（本次）：`F_B` 45 → **62**（+kind 留位 3 + 城堡逐级造价 5 + 效果槽 9）、新增地形组 `t`
（`F_T=4`：减伤/造价惩罚 + 2 维留给地形改版的"崎岖降速"）、`ARMY_FEAT` 10 → **12**
（军队行自带 speed/atk）、网格 +1 条 `build_cost`（地形惩罚×工程院减免，模型不必自己乘）。
词表再添地形留位 2 个（`V.TERRAIN` 5→7）。

**新的口径**：网格 **54**（归属段从 2 槽钉成固定的 11 槽）、glob **58**、token **352**、`F_A`=**16**、`ARMY_FEAT`=**12**、
`F_B`=**62**（kind one-hot 14 项 = 11 真实 + **3 留位**，地形改版计划的「港口」这类新 kind
直接填空位，`F_B` 不变）、`F_T`=**4**。`jitter` 现在也抖 effects 与 TERRAIN_STATS（cap 类守下限 ≥1，
地形 defense 可为负）。

### 当时的候选方案（留档，已选 B）

**(A) RL 侧镜像效果表**（便宜、不碰契约）
在 `rl/effects.py` 里按 `kind` + 引擎常量构造每建筑一条**效果向量**，例如：
`[防御%/级, 市政厅基础产金, 每 slot 产金, 建造减免%, 视野半径, 解锁类型 one-hot(无/兵营/军屯), 外交减免, 预留…]`
≈ 10 维；再补地形组 `t`（`defense/build_penalty` + 2 维预留，为地形改版计划的"崎岖降速"占位）
与城堡逐级造价（5 维）。
**代价**：镜像**会漂**（引擎改了常量、镜像没改就是静默错），所以要配一条
「镜像 == 引擎常量」的一致性测试。
**范围**：`F_B` 45 → ~60、新增 `t` 组 7 条、`ARMY_FEAT` 10 → 12（军队行补 speed/atk）、
token 345 → ~352。

**(B) 把效果搬进 `game.BUILDINGS`**（正解，但动 main）
让效果像 `outputs` 一样**成为数据**：`{"kind": "academy", …, "effects": {"build_discount": 25}}`。
这才是"更宽泛 4X"的地基 —— 新建筑只要写数据就能被表达，模型不必为它重学。
**代价**：`game.py` 是引擎文件（契约：与 main 逐字相同）→ 这是 **main 侧的重构**，
要单开一炉；而且 `mp.py`/`mp_ai.py`/`spend_rules.py` 都要改成读这个新字段
（否则又是"数据在表里、逻辑读常量"的半吊子）。

**(C) 顺带必须处理的**：`jitter` 要能抖这些效果常量，就得**同时写 `game.X` 与 `mp.X`**
（`mp.py` 是 `from game import ENGINEER_DISCOUNT`，值拷贝，只改 `game` 那边引擎看不见）。
这不是改文件（契约测的是字节），是运行期绑定 —— 要在 `jitter.py` 里列一张"别名表"。
同理 `spend_rules.SUPPLY_PER_ARMY` 是硬编码副本，所以 `unit.supply` 至今不抖（§10.4）。

### 10.8 不做的事

- 不把「网格每建筑一条通道」解耦掉（那要重做 M 组语义；本版用**留位**顶住，够用几轮）。
- 不动 `mp.py`/`game.py`/`mp_ai.py`/`mp_run.py`（契约：与 main 逐字相同）。
- 不动 §9 记忆的实现、不碰 `KIND` 的 32 与 `F_A` 的第 12 维。
- 不随机化可行性拓扑类字段（`cap_resource`/`limit`/`min_slots`/`max_level`）与 `unit.supply`。

### 10.9 岔路：**已全部拍板**（2026-09-12）

| 岔路 | 拍板 |
|---|---|
| 数值进哪 | **两处都做**：新 token 组 `b`/`u` + 候选 `se` 带内容 |
| 留位规模 | **少留，但三类都留**：建筑 16→**20**、兵种 3→**5**、商品 6→**8**（10.3） |
| 随机化 | **默认关**；训练用 `--rules-jitter 0.2` 显式开；评估/看海一律真值 |
| §9 的 +2 通道 | **并进本版**（只占宽、恒零；将来填值不再变形状） |

**没有未定项了** —— 这份清单可以直接照着开工。开工前只剩一件事：10.1 的基线数字
要在动手前**再实测一遍**（防的是"我写完之后有人又改了 main"，那一版数字是改动的锚点）。


---

## 10.11 哪些改动会改宽度？（2026-09-12 定稿 —— 下次改动先查这张表）

问的是「加 X 要不要重炼」。答案分两层，**别混**：

- **形状**（ckpt 能不能加载）：留位解决的。宽度一变，`load_state_dict` 当场报错。
- **能力**（模型会不会用）：训练解决的。新东西即使形状不变，那几维此刻是**死权重**。

所以「要训练」和「要从零重炼」是两件事：形状不变 ⇒ 可以**热启动接着训**。

| 改动 | 网格 | glob | token | 嵌入表 | 结论 |
|---|---|---|---|---|---|
| 改**数值**（造价/产出/效果/地形/价格） | — | — | — | — | **不必重训**（数值进输入 + 域随机化已覆盖） |
| 加**建筑**（填留位槽） | 不变 | 不变 | 不变 | 不变 | 形状不变 ⇒ 热启动 |
| 加**兵种 / 商品 / 地形 / kind** | 不变 | 不变 | 不变 | 不变 | 形状不变 ⇒ 热启动 |
| 加**对手** | 不变（归属 11 槽） | 不变（对手段已删） | 不变（N 组 8 槽现成） | 不变 | 形状不变；但 per-国信息要等 N 组填值（§9.5，与加对手同炉） |
| 加**记忆**（§9） | 不变（`remembered`/`probe` 已占） | 不变 | 不变（E/K/R 槽、A 组 age 维都现成） | 不变 | 形状不变 ⇒ 热启动 |
| 加**外交动作** | 不变 | 不变 | 不变 | **会变**：`type_emb` 8→28、sub 表 8→28 张 | ★**要重炼**（用户 2026-09-12：外交「留位就行」—— 那一炉本来也要训新能力，不为它提前接线） |

**两条守门测试**（谁把动态逻辑写回去就红）：
- `test_observation_width_invariant_to_rivals`：`rivals=()` 与 8 个对手都是 **网格 54 / glob 58**
- `test_vocab_main_parity` 的「main 是活跃前缀」系列：留位只许加在表尾

**两条待接线**（今天没做，记在这里免得当成"已经能了"）：
1. 外交的嵌入表仍按 `env.KINDS`(8) 建，**没有**按 `vocab.KIND`(32)。加外交那一炉要从零。
2. N 组 token（8 槽 × 16 维）骨架在、`mask` 恒 0 —— per-国信息**还没有**任何生产者。

---

## 10.12 候选集放开 + 上一步的反馈（2026-09-12 用户口径，**成对落地**）

### 一、候选集 = 引擎的合法动作空间（`rl/env.py`，引擎一行没动）

用户原话：「你应该**去掉可选集**，因为模型可能根本不知道兵营价格，连**攒的目标**都没有」
+「只修 env，token 应该只输入游戏状态，而不是帮忙计算」。

删掉的是**按钱/货的预过滤**：`build` 的整套造价计算（地形惩罚/工程院/匈奴）+ `gold < cost`、
`recruit` 的 `res // amt`、`buy` 的 `total <= gold`、`sell` 的 `amt <= res`。
**保留结构约束**（位次/上限/所需资源/每地块每回合一次）—— 那些是"哪儿能建"，不是"买得起"。

两条理由：① 买不起的建筑以前**连候选都不生成** ⇒ 模型看不见目标、没有攒钱的目标；
② "候选存在"本身**泄露一比特**「我此刻买得起+合规」，与契约第 2 条冲突。
（顺带修掉一个旧毛病：env 以前比引擎**还严** —— 引擎允许 `build` 失败并如实报错。）

**代价（实测，沿老师轨迹）**：候选数中位 **256**（p90 366 / 峰值 396），
其中 **49% 是旧口径会砍掉的** ⇒ 候选数**翻倍** ⇒ 打分头 `O(K·T·d)` 约 **+19%/步**
（一局 9.6 → 约 11.4 min）。

### 二、上一步的反馈进观测（**必须成对**，否则上一条就是净损失）

候选放开后 ~49% 的候选是"点不动"的。**没有反馈**，策略分不清「点了无效选项」与
「做了中性动作」—— 两者对它都是"什么都没发生"（PPO 的 advantage 也分不开），
**学不出甄别**。而这份信息**玩家本来就有**：LLM 玩家的工具返回值就是这个文案
（`mp.py` 的 `return False, f"..."`），留在它上下文里 ⇒ 编码它**不是"帮模型计算"**。

落地：`G 组` 末尾加 **9 维** —— `last_ok`(1) + 被拒原因 one-hot(8)：

    REJECT_REASONS = (黄金不足, 木材不足, 物资不足, 结构, 军队, 领土, 停摆, 其他)

成功时全 0（`last_ok=1`）；映射在 `rl/features.py:reject_reason()`（关键词表，顺序敏感：
先判"停摆"，因为"全国电网不足…停摆"里也含"不足"）。
**防漂测试**：`test_reject_reason_covers_every_engine_message` 从 `mp.py` 里把七个动作内
**全部**拒绝文案抽出来逐条归类，断言**没有一条落进「其他」** —— 引擎改文案而映射没跟上就红。

### 三、新的宽度

| | 旧 | 新 |
|---|---|---|
| glob | 58 | **67**（+9：上一步反馈） |
| `g` token 宽 | 66 | **75** |
| 网格 / token 总数 | 54 / 352 | 不变 |

⇒ **宽度变了 ⇒ 要重炼**（这是"改动让 ckpt 失效"那一类）。

---

## 10.13 坐标系对齐：候选落点也改成「相对家」（2026-09-12）

**问题**（用户问「老师是按绝对坐标还是相对坐标的，有没有转换」时查出来的）：

| 路径 | 坐标系 |
|---|---|
| 老师 → 引擎 | **绝对**（1-based 工具参数 → `bc.to_action` 转 0-based，仍是绝对）→ `match()` 对候选 | 
| token（M 组 patch 中心、A 组军队） | **相对家**（`tokenize.py`） |
| 候选落点特征 | **相对可见区外接框**（`env._cand_pack` 的 `tile_dx = a.tile[0] - x0`） |

⇒ **标签路径没有问题**（全程绝对、无相对化）；但**观测里两套相对坐标系同尺度、不同原点**：
模型得自己学出一个**每帧变化**的偏移量（家 − 框原点）才能把"候选在哪"和"patch 在哪"对上。
原则上可推（patch 坐标的展布透露了偏移），但纯属白费劲。

**改法**（`env` / `ppo` / `transformer` 三处，引擎不动）：候选**新增一对** `tile_hx/tile_hy`
（相对家，模型特征用），**保留** `tile_dx/tile_dy`（框内相对，拼批算扁平下标用）——
两套并存，各司其职。`cand_pos_block` 改读 `tile_hx/hy`。

**测试**：新增 `test_候选落点与token同原点`（逐候选核对 `tile_hx == tile − anchor`，
无落点必须是 `(-1,-1)`）；两条手改落点特征的用例改用新字段。

**不动**：`POS_SCALE`（尺度本来就一致，只是原点不一致）。

---

## 10.14 ★新增 C 组（物资 token）：**一条 token 一种货**（2026-09-12 晚）

**症状**：`sell` + `buy` 占标签 **68%**，而模型在**最大类 `sell` 上只有 0~6% 命中**
（在一张全新的老师局上量；`experiments/bc_class_hit.py`）。

**静态诊断**：这两种候选要做的判断是「**我这种货有多少 / 现在什么价 / 均衡价多少**」，
而这三样**只住在 G 组那唯一一条 token 里**（`glob` 的 `res:*`、`price:*`、`eq:*`）。
cross-attention 对**单条** token 的 softmax 只有一项 ⇒ 权重恒为 1 ⇒
**候选的 query 影响不了它从 G 里读到什么**；候选只能靠 `score` MLP 在
`[h, q0]` 上硬学"按 sub 选对应那一维"。**这是原来表达不出来的运算。**

**改动**：新增 token 组 `c`，`CAP["c"] = len(V.SUB_TABLE_OF["buy"]) = 8`（6 真 + 2 留位），
每条 `F_C = 3`：

```
[我的库存/1000, 现价/基础价, 均衡价/基础价]
```

- **一条 token 一种货** ⇒ "我这种货"变成一个**可被注意到的对象**，query 里带着货物身份
  就能选到它 —— 与 `b`/`u`/`t` 三组同构（那三组各是"一条 token 一个实体"）。
- 与 B/U/T 的区别：那三组装**规则表里不随时间变的数**（造价/产出/减伤），
  C 组装**每回合都在变的局面数**。

**★只放 glob 里已有的那三样，一格不多**。理由：
1. 契约 §2「信息集对齐」要求每个 token 组都能在 main 的面板里找到对应项。
   `flow_in`/`flow_out`（世界产耗）**刻意没进 glob**、且每回合末清零 —— 那是引擎
   内部状态，放进来就是泄漏，所以**不放**；
2. 缩放与 G 组**逐位一致**（同样 `/1000`、同样 `/基础价`），同一段数在两处同尺度。

**宽度影响**：token 合计 **352 → 360**（预算 512）；参数量 2,109,570 → 2,110,530
（`+960` = 8 token × 投影 768 + 组身份嵌入 192）。⇒ **本改动之前的 ckpt 一律不可续**。

**未做**：per-good 的产/耗（`flow_*` 是引擎内部量，见上）；把 G 组里那三样摘掉
（先并存，跑一炉看 C 组有没有被用起来再谈瘦身）。
