# 战国 RL 战术线（`feat/rl-tactics`）· 计划

> **2026-09-24 从零起**，基座 `68d40b1`（09-23 的 `main`，即最新引擎）。
> 旧线（"端到端替代 LLM"那条）封存在 `feat/rl` @ `5260224`，tag **`rl-archive-2026-09-24`**；
> 它的备忘在**那条分支的** `rl/PLAN.md` 顶部封存节。本线**不继承旧线任何 ckpt**（基座不同）。

---

## 一、为什么换线

**旧线**：**一个**模型吃全观测 → 吐引擎动作空间里的**任意动作**（建造/征兵/买卖和移动/攻击
混在同一空间），目标挂**终局总消费**。病根已量化（`--grad-diag`）：`coh(pg)=0.0635`，
噪声底 `1/√230=0.0659` ⇒ **回报信号对更新方向没有系统贡献**；`cos:pg:bc=−0.778`（与 BC 锚互相拆台）。

**新线**：**每个目标单独炼一个模型**，问题切小 ⇒ 动作空间小、判据清楚、每个模型都小到能快跑。

---

## 二、任务库（7 个，用户 2026-09-24 定）

| # | 组 | 任务 | 目标（判定） | v11plus 现在会吗 |
|---|---|---|---|---|
| 1 | 占地 | 快速控制无军空地 | 抢占看得见的空目标格 | ✅ 候选池**源①**（`need=1`） |
| 2 | 占地 | 打野扩张 | 打掉野人守军并占地 | ✅ 候选池**源②**（`need≥2`） |
| 3 | 占地 | 夺取领土 | 夺**敌国**地块 | ✅ 候选池**源③** |
| 4 | 守土 | 保卫领土 | 己方地块不被夺 | ❌ 底稿 §十 把"守土/驻防/撤退"留空 |
| 5 | 守土 | 保卫国祚 | 市政厅不被拔光（拔光即亡国） | ❌ 军事层是 09-16 的，不知道国祚 |
| 6 | 破敌 | 歼灭 | 打光对手野战军队 | ❌ 只打格子，不追军队 |
| **7** | 破敌 | **攻取国祚** | 拔光对手所有厅（灭国） | ❌ 同上 ← **★ 本线从它开始** |

**基线分两档**：1~3 有现成老师（要**超越**）；4~7 基线是**零**（要从无到有）——后四个
正好是**国祚层的攻防**，也就是"只能靠对抗式"那部分（没有老师可模仿，BC 从根上不成立）。

### 国祚（市政厅）的口径 —— 2026-09-21 起，`2d66417`

- **国祚 = 市政厅**：`has_townhall` = 名下有**已落成**的厅（在建不算）；
  `_eliminate_if_dead` **厅尽失即亡，领土还在也照亡**。
- **开局白送**：`_place_crosses` 在十字开局**中心格**送一座（不花钱、不受 `min_slots≥6` 约束），
  中心格**强制平原**。⇒ **每个国家开局就有一座厅**，场景天然有目标。
- **亡国余土成无主废墟**：`owner`/`core` 置 None、**建筑原样留着**、**不补野人守卫** ⇒ 废墟**白捡**。
- ★ **市政厅对他国公开**（`_public_buildings`：视野内的他国地**只公开城堡与市政厅**）——
  原话「**看不见就打不着**，不公开这条规则就是死的」。

---

## 三、★ 首个沙盒：攻取国祚（8×8 自对弈）

**用户 2026-09-24 原话**：「直接 ppo 对抗，例如一个 8x8 的地图，放两个国家，开局每人 5 个步兵，
每五个回合补一支，**奖励是拿下对手的市政厅**，**开局告诉模型自己市政厅和敌人市政厅的位置**，
**稀疏奖励**。类似 alpha zero 那样训练。」

### 3.1 规格

| 项 | 值 |
|---|---|
| 地图 | **8×8**，两国（甲 / 乙） |
| 开局 | 每国 **5 个步兵**，摆在**自己的核心格** |
| 补员 | **上限 = 5 + 本国国土数 // 10**（开局 5 格 ⇒ 上限 **5**，与"开局 5 步兵"自洽；8×8 最多 32 格 ⇒ 上限 8）。**每 5 回合触发一次**：把军队**补足到上限**（★不是"每次 +1"、**不无限加兵**）。补在核心格，规则白给、**不经经济层** |
| 国祚 | 每国核心一座市政厅（`_place_crosses` 白送） |
| **终局** | **一方拿到对手的市政厅** ⇒ 本局立即结束（**不设固定回合数**） |
| **兜底** | **T_max = 200 回合**（只防僵局；用户 2026-09-24：「40 回合不够」）⇒ 到时判平 |
| **奖励** | **赢 ⇒ `1 − turns / T_max`**（★用户：「时间越短奖励越高」）；**输 ⇒ −1**；**平 ⇒ 0** |
| **观测** | ★ **POMDP**：**市政厅位置公开**（引擎 `_public_buildings` 口径 —— 视野内的他国地**只公开城堡与市政厅**，"看不见就打不着"）+ **其余一律按视野 `visible_to`**（军队 / 资源 / 其它建筑都是内政底细，得靠侦察）。⇒ **目标已知、守军未知** |
| 野人 | **清空**（`guard_once` 填满全图防重生）——本沙盒里没有野人 |

★ **迷雾的战略含义**（用户 2026-09-24 纠正后补）：守军未知 ⇒ "偷家"必须先知道**对方家里空着**，
**侦察（推进视野）本身就有价值**；反过来，"先手必败"那个观察（8/8 乙赢）在迷雾下**要重新审**
—— 后手"看到空档"这件事本身就受视野限制。
另：嫁接 v11plus 军事层**天然带这个口径** —— `targeting.candidates` 的**每一源**都要过
`cell in mask`（`pathfind.vision_mask`），它从不读视野外的军队与资源（v9 当年堵掉的四处越权之一）。

### 3.2 实测过的可行性（2026-09-24）

```
World(size=8, seed=0, nations=['甲','乙'])
  → tiles = 10（两国各 5 格十字）
  → 甲 市政厅 (4,2)   乙 市政厅 (4,6)      ← 自动送的，不用手搓
  → 开局军队 = 0（要自己摆）；野人 = 54（要清）
```
- ✅ `w.armies` 过滤 + `w.guard_once` 填满全图 ⇒ 野人剩 **0**
- ✅ `_new_army(owner)` + append ⇒ 摆得出 5 步兵
- ✅ `resolve_turn()` 跑通
- ⚠ **坑**：军队别摆在 `own_tiles[0]`（字典序第一格，实测落在 `(3,2)`，**不是核心 `(4,2)`**）
  ⇒ 用**市政厅所在格**当出生点。
- ⚠ **待查**：`w.turn` 在 `resolve_turn()` 之后没递增 —— 回合计数的口径要单独确认。

### 3.3 动作（军事子集）与 **候选集屏蔽**

每回合**逐军决策**（一支军一次）：`move(army_id → 格)` · `attack(army_id → 格)` ·
全部动完即 `end_turn`。**砍掉** `build`/`recruit`/`buy`/`sell`（沙盒里兵是白给的，经济层整个不进）。

★★ **候选集屏蔽**（用户 2026-09-24：「这次要做候选集屏蔽，**无法移动的位置屏蔽，
已经使用过的军队屏蔽**」）—— 在 `sandbox.legal()` 里做，**进模型之前**：

| 屏蔽什么 | 判据 |
|---|---|
| **走不到的位置** | 落点只从 `world._reachable` 取 ⇒ 根本不给 |
| **已经使用过的军队** | `a["moved_turn"] == world.turn` ⇒ **整支屏蔽**（一个动作都不给） |
| **交战中的军** | `a["engaged"]` ⇒ 同上（它动不了、也不能改攻） |
| **撞墙的 mv** | 结构上不存在（落点来自引擎合法集，不会触发 `_blind_cost` 烧额度） |

★ **这与旧线是反着来的**，值得记牢：旧线的候选是「引擎合法动作**全部**、不限额不预过滤」，
实测 **~49% 点不动**，靠一个 `exec_head`（可执行性头）去学"这个候选点不点得动"。
**屏蔽之后那个头就不需要了** —— 候选集小一个量级、**无废动作**、学习信号不被稀释
（旧线 `coh(pg) ≈ 噪声底` 的病根之一，正是大量废候选在稀释梯度）。

---

### 3.4 **打分器**（替代 BC，也替代 MCTS —— 用户 2026-09-24 定）

> 用户原话：「要是搜索也写不好就**纯打分**，越近敌方分越高，越多地分越高，丢地扣小分，
> 损兵扣大分，丢家直接输」＋「消灭敌军也要加分」＋「每多一支军队也加分，鼓励占地盘加兵」
> ＋「**有打分器的话就不用 bc 了，直接 ppo 对抗**」

`rl/evaluate.py` 给一个**静态局面**打分（正 = 我占优）。逐条落地：

| 项 | 权重 | 用户口径 |
|---|---|---|
| 丢家（国祚尽失） | `±INF` | 「丢家**直接输**」 |
| 国土 | 每格 `±1` | 「越多地分越高、丢地扣**小**分」 |
| **我的兵** | 每支 `+10` | 「损兵扣**大**分」＋「每多一支军队也加分」 |
| **敌的兵** | 每支 `−6` | 「**消灭敌军也要加分**」 |
| hp | 每点 `±0.02` | 伤而不死只是小账 |
| 逼近 | 每近一格 `±1.5` | 「**越近敌方分越高**」 |

★★ **两条设计要点**：
· **兵分两项写、不合成"兵力差"** —— 合成的话 1 换 1 会抵消成 0 分，"消灭敌军"就白干了。
· **权重与引擎规则天然对齐**：引擎是「每 10 格国土 ⇒ 补员上限 +1」，而这里是
  「10 格地（10 分）= 1 支兵（10 分）」⇒ **"占地 → 上限涨 → 兵多"这条链已经是通的**。

★ **它一物两用**：① PPO 的**势函数**（奖励取它的差分）；② 将来价值网络的**老师/对照**。

（~~纯打分贪心基线~~ **已删** —— 用户 2026-09-24：「那玩意**证明不了真正的对抗**」：
贪心 vs 贪心是**同一个确定性策略对打自己**，既不对抗、也不学习，拿来当"基线"什么也说明不了。）

---

## 四、训练方法：**PPO + 自对弈 + 打分器**（用户 2026-09-24 定）

| 决策 | 选择 | 说明 |
|---|---|---|
| 自对弈 | **两份独立网络**（`W_甲` vs `W_乙`） | 互相当对手；对手在动（非平稳），但能防"自己克自己" |
| 搜索 | ~~MCTS~~ **不用** | ★ 用户：「有打分器的话就不用 bc 了，**直接 ppo 对抗**」—— 打分器已经把中间信号给足 |
| BC | ~~不要~~ | 同上：不需要老师带起步 |
| 奖励 | **打分器的差分** | `reward_t = score(s_{t+1}) − score(s_t)`；终局 = ±1（丢家直接输） |

★★ **为什么打分器能把 BC 和 MCTS 一起省掉**：稀疏奖励的真问题是"值函数要从零学
二十回合后会不会赢"，而 PPO 的优势估计又依赖值函数，两者会互相拖。打分器把这个死结
**从根上消掉** —— 它给每一步一个密集信号，等于把终局判据**提前铺到每一步**。

★ 而且它是**势函数差分**（potential-based shaping）：与旧线的
`reward = Δspend_total × 0.01`（`feat/rl` 的 `rl/env.py:336`）**同形** ——
理论上**不改变最优策略**，所以既不引入 reward hacking，也不必担心"塑形把策略带偏"。

★ **奖励的时间项与补员规则是咬合的**：每 5 回合补员 ⇒ 拖久双方都补满、更难突破
⇒ 速攻本就更优，而「时间越短奖励越高」正好把这个压力灌进梯度。

★ **便宜且强的技巧**：两国**完全对称** ⇒「甲视角」与「乙视角」是同一批样本的两份
（视角互换做数据增强）—— 样本翻倍，且**强制策略对称**。

---

## 五、复用与重写（对旧线 `feat/rl` 的取舍）

★ 这些文件在**旧线分支**上，本分支**没有 `rl/`** —— 取法 `git checkout feat/rl -- rl/<f>`。

**可复用**：`rl/ppo.py` 的 `Rollout`/GAE/`collate*`（数学）· `rl/workers.py`（并行收集）·
`rl/train.py` 循环骨架 · `rl/features.py`/`tokenize.py`/`vocab.py`（编码方式）。

**★ 模型侧：裁剪旧线的，不重写**（用户 2026-09-24：「模型就用上次的模型，只是**去掉大量
原有的维度和 token**，**改造分词器**」）—— 搬 `transformer.py` / `model.py`，
**砍掉沙盒不需要的维度与 token**（经济、外交、建筑那些），改 `tokenize.py` / `vocab.py`。

**要新写**：环境（本沙盒小到可以直接对着引擎写）· 观测编码 · 自对弈回路 · PPO 训练循环。

**不搬**：任何 ckpt · `rl/bc.py`（**不用 BC 了**）· `rl/ruleai_bridge.py`（自对弈不需要老师旋钮）。

---

## 六、目录（建议）

```
rl/
  PLAN.md        本文
  sandbox.py     8×8 沙盒：建局 / 宣战 / 摆兵 / 补员 / 终局 + **环境接口**（legal/step/clone）
  evaluate.py    ★ **打分器**（势函数 / 贪心基线 / 将来网络的老师）
  encode.py      观测编码（★按视野：棋盘 + **可见**的军队 + 公开的双方市政厅）
  military.py    （"手写决策"那版，**已证伪**；留档，不走主路径）
  mcts.py        （搜索；实测 40 sims 不如纯打分，**暂不走主路径**；留档）
  encode.py      观测编码（★按视野：棋盘 + **可见**的军队 + 公开的双方市政厅）
  net.py         策略/价值网络（★**裁剪旧线的** `transformer.py`/`model.py`，不重写）
  arena.py       自对弈回路（两份独立网络）
  ppo.py         PPO（从旧线搬数学部分）
  train.py       训练循环
```

---

## 七、路线（一版一 commit）

**阶段①（本沙盒，先跑通）**

1. ~~沙盒跑通~~ ✅ **已完成**：能跑完一局并正确判定胜负；踩平三个坑（`begin_turn` /
   补给 / 野人必须留）。
2. **打分器** ✅ **已完成**：`evaluate.py` 按用户规格落地。
   （~~纯打分贪心基线~~ **已删** —— 用户：「那玩意**证明不了真正的对抗**」。）
3. **网络** ← **下一步**：★ **裁剪旧线的** `transformer.py`/`model.py`
   ＋ 改 `tokenize.py`/`vocab.py`（去掉沙盒不需要的维度与 token），接上沙盒观测。
4. ~~脚本老师 + BC~~ **不做**（用户：有打分器就不用 BC）。
5. ~~MCTS~~ **暂不做**（实测 40 sims 不如纯打分贪心）。
6. ~~自对弈回路~~ / 7. ~~PPO 训练~~ ✅ —— **合并在 `rl/train.py` 一个文件里**
   （沙盒小，不需要旧线那三件套 `ppo.py`/`workers.py`/`train.py`）。
   ★★ **实测 30 轮：真的学起来了** —— 平均回合从随机策略的 51~72 降到 **5.8**
   （≈ **理论最短**：两核心距 5、步兵 1 格/回合 ⇒ 最快 5 回合到达），甲胜率 0 → 3~4/4，
   `ent` 2.58 → ~2.0，`pg` 稳在 −0.03~−0.05。
   ⇒ **纯 PPO + 打分器势函数 + 自对弈，无 BC、无 MCTS**（用户那条判断成立）。
   ⇒ 也验证了「拿下市政厅即结束 + 时间越短奖励越高」这组设计咬合得好：5.8 回合是被时间项逼出来的。
   ⚠ **但 8×8 已到天花板**：甲胜 3~4/4、乙胜 0~1/4 ⇒ **先手优势是结构性的**（步兵 5 回合直达，
   回防不可能、没有防守余地）。这正是用户那句「训练从简单的开始**不代表模型就应该只对标简单的**」
   —— 下一步按 §11 扩到更大图（40×40、20+ 军）才有防守/协同的空间。
8. **评估**：对随机 / 对**纯打分贪心**（★ 真正的对照线："PPO 打败了势函数贪心吗"）/
   对上一版自己；★ 报数说清覆盖到哪一段。
   ★★ **用户 2026-09-25 的裁决**：「**`rl/military.py` 那个老师不能代表任何东西，
   这个老师不会打仗，也不作为基线**」⇒ 它是沙盒自检 / `rollout` 用的"能走两步的老师"
   （**别拿它的成绩报数**）；要对照就对**纯打分贪心**。
   ⚠ 历史记录：它曾**静默返回零动作**（`rollout` 200 回合 0 动作判平、不报错）——
   根因是 `targets(defend=True)` 把**自家核心**（**零代价**目标）放进候选池 ⇒
   最小代价指派把全军认领自家核心；顺带查出"家里告急⇒收缩池子"那段筛了 `cells`
   却把**筛之前**的 `cands` 喂给 `_solve` ⇒ **守家特性从没生效过**。两个都已修
   （`rl/military.py`、常量 `DEFEND_R`），留一条最小守卫（只钉"不是 no-op"）。

**阶段②**：换真对手（v11plus）验"学会了能不能打真对手"。
**阶段③**：其余 6 个任务，以及同局多模型协同（**独立课题，阶段① 不背**）。

---

## 八、待定（需要用户拍）

1. **兜底上限 T_max = 200** 行不行？（到时就判平 0 —— 或者改成"按国土数判胜负"）
2. **`retreat` 要不要**进动作集（引擎有这个动作，`RETREAT_ATK_PENALTY=80`）。
4. **地形**：用引擎随机地形（影响移动代价与防御）还是**强制全平原**（更纯的对抗）？
5. **MCTS 与 PPO 的结合形式**：我按"**搜索分布当策略目标 + PPO 的 ratio 目标**"理解
   （而不是 AlphaZero 原版的纯交叉熵），请确认。
6. 训练算力：本机小规模验证管线，还是直接上 ECS / 免费 GPU 机。

---

## 九、环境备忘（别踩）

- **引擎动作是原子的**：`attack` 里任一支到不了 ⇒ **整通调用全废** ⇒ 出手前必须逐支复核
  （`ruleai/v11plus/military.py` 就是这么做的）。
- 落点从 `world._reachable`（引擎合法集）里选 ⇒ 结构上不会撞墙。
- **8×8 开局就有 54 个野人**（`_ensure_guardians` 铺满）—— 本沙盒必须清。
- 测试**绝不读写 `mp_save.json`**（铁律）。
- 多国局在**引擎侧**是现成的（`mp_run.py` 支持 `cfg["nations"]`，无 key 国家由规则 AI 代打）；
  但**自对弈不需要它** —— 本沙盒自己驱动双方。

---

## 十一、主干换 `WindowTransformer`（**可扩展性**）

> ★ **2026-09-24 夜：本节的改动已全部落地**（见 §12.1）。落地时的三个偏离，逐条记：
>  · `rl/transformer.py` 在本分支**根本 import 不了**（依赖旧线的 `rl.env` /
>    `SubEmbedder`，二者都不存在）⇒ 是**重建**、不是"接线"，原文件**留着不动**当参考。
>  · **CNN 没退场**：本节表里"候选空间特征按目标格 gather"那行是"**同**"⇒ 保留。
>    窗口只管**军队 token 组**（`g` + `a`，结构留位可扩展）。
>  · ★ **归一尺度放在 `encode` 侧**，**没做成模型上的 buffer** —— 一批里会混
>    不同尺寸的地图（随机地图），单值 buffer 会**静默**对不上尺度。
>  · 顺带修掉三处**静默丢信息**：归属 6 类（中立国≠敌国）· 盟友厅/盟友市政厅 ·
>    `hold` 被当成 `attack`（`row[0 if kind=="move" else 1]`）。


用户 2026-09-24：「**真实对战经常是 40×40 的地图、20 多个军协同**，训练从简单的开始
**不代表模型就应该只对标简单的**」。

⇒ **8×8 是跑通管线的起点，不是模型容量的对标物**。我一度按 8×8 定制了模型
（全局池化、均值池化军队编码、还把 `transformer.py` 删了），那是短视。要改对的几件：

| 项 | 现在（8×8 定制） | 改成 |
|---|---|---|
| **候选编码** | 12 列平铺标量 | ★ **下标形态**（`type_idx`/`sub_idx`/落点/`army_idx`）—— 旧线形状 |
| 候选空间特征 | 按目标格 gather（已修） | 同（旧线本来就有，用 `tile` 那一槽） |
| **候选互见** | ❌ 无 | ★ **`cross2`** —— "K 个里选一个"本质是**相对比较**，独立打分表达不了 |
| **规则表数值** | ❌ 没进输入 | ★ **`SubEmbedder` 第二路**（查表 + 规则表内容）—— 引擎改了造价/数值，**同一盘面的决策要跟着变**（旧线栽过：石油厂 240→180 时模型动作一模一样，而最优解已经变了） |
| **军队集合** | 均值池化（20+ 军会糊成一团） | ★ **注意力池化**（或直接进窗口走 transformer） |
| **窗口分组** | 1 组（军队） | 旧线 7 组（`g/m/a/n/e/r/k`）—— 沙盒先只开军队，**结构留位** |
| **地图尺寸** | `POS_SCALE=8`、几处 `/8.0` 写死 | ★ **参数化**（从 `sandbox.size` 取） |

★ **适配路径**：`encode.py` 的候选部分改回**下标形态** + `win` 用军队 token 组
⇒ 直接喂 `WindowTransformer.forward(win, cand, cand_mask)`，**主干一行不改**
（旧线那些注释里的坑——padding mask 的 `~wmask`、全 padding 行的 NaN 兜底、
`cand_mask.to(device)`——全是踩出来的，照用）。

⚠ **不影响当前能跑的基线**：`train.py` + 平铺版 `model.py` 仍然可用（实测 3 轮跑通），
   改造是**增量** —— 先把旧主干接上跑起来，再对比两条主干。

---

## 十二、状态与待做（2026-09-24 夜 · 交接）

★★ **接手先读（2026-09-25 08:30 交接）**：

1. **正在跑什么** —— ECS 上 `tmux rl` 跑自对弈 PPO：**8×8 / 3 国 / 上限 500 / 池子 5 份**，
   **每 5 个 iter 定时重启进程**（抗内存增长，§12.9），从 `rl/runs/ppo/base_0925_small.pt`
   续跑。查/停：`ssh ecs` → `./rl/run_ecs.sh status | logs | kill`。
   ★★ **曲线（它真的在学）**：`ent` **3.10 → 1.8~2.6**（纯均匀 = 3.16），
   平均回合 **24 → 16 → 7.5** ⇒ 既收紧了策略、又把局打快了。
   ★ 报告口径：**"平均回合"是每 iter 现抽的随机地图**，逐 iter 比它是噪声；
   要干净判据用 **`rl/eval_fixed.py`**（固定种子、双臂对照，§12.9/提交 `ee37df6`）。
2. **口径（改任何一项先套这几条，全是用户逐条拍的，别再自己发明）**：
   §12.4（打分构成）→ **§12.4'（总口径：只对自己国家负责 / 除威胁外不许读敌人 /
   它是脚手架要拆、最终只看胜场）** → §12.4''（**训练规则推出来的量不许进观测**）→
   §12.8（**病因是状态空间爆炸，不是奖励稀疏**）→ §12.9（定时重启）。
3. **还没做的**：§12.2 第 4~6 条（联赛池"只增不删"那一半 / 批量操作 / 吞吐）。
4. **坑**：§12.3（`cross2` 必须分 minibatch / `REWARD_TANH_SCALE` 要锚量纲）、
   §12.5（战斗明细三条口径 + 每步成本）。

### 12.1 ★ 本轮已完成（都在 `feat/rl-tactics`，逐条 commit）

| # | 件 | 落点 |
|---|---|---|
| ✅ | **战斗概率推演**（精确、不需蒙特卡洛） | `rl/combat_probs.py` + `tests/test_combat_probs.py` |
| ✅ | **主干换成窗口 Transformer**（cross2 / 下标形态候选 / 规则表内容 / 参数化归一） | `rl/model.py`、`rl/encode.py`、`rl/vocab.py`、`tests/test_rl_backbone.py` |
| ✅ | ★ **规则表内容向量**（现算不缓存；军队 token 挂兵种数值） | `rl/features.py` + `tests/test_rl_features.py` |
| ✅ | ★ **打分先验表**（所有数字抽出来，`override()` 可扫参） | `rl/scoring.py` + `tests/test_rl_evaluate.py` |
| ✅ | **国祚的两种可见模式**（"已派间谍"已知 / 自己找厅） | `sandbox.halls_known`、`--halls-known` |
| ✅ | **PPO minibatch**（修 OOM 崩溃，见 12.3） | `rl/train.py`、`scoring.PPO_MINIBATCH` |
| ✅ | ★ **战斗明细进观测**（概率/轮数分布/增援/撤退，**每帧重算**） | `rl/combat_probs.frame_odds`、`rl/encode.py`、`vocab.GRID_CB_*`/`A_CB_*` |
| ✅ | ★★ **市政厅的永久记忆**（用户：「发现厅了就应该**永久标记**，因为厅是**拆不掉也不能移动**的」） | `rl/hall_memory.py`、`sandbox.known_halls`、`evaluate`/`encode` 的 `known` |
| ✅ | ★ **守家项从「逼近项」里拆出来**（原来共用一个 `if my_halls and foe_halls`） | `rl/evaluate.py` |
| ✅ | ★ **盟友的厅纳入同一本"已知"账** + 修掉网格里 `GRID_HALL_*` 的**真闪断** | `rl/evaluate.py`、`rl/encode.py` |

**测试 635 → 718**，全过；两种模式各跑通冒烟；主干 **1.382M** 参数（用户要求 ≥1M）。

### 12.1' ★ 2026-09-25 本轮（多玩家 + 击杀口径 + 起炉）

| # | 件 | 落点 |
|---|---|---|
| ✅ | ★★ **多玩家**：`n_nations = f(size)`∈[3,5]、逐实例玩家表、**全对宣战**（默认无中立/不结盟）、先手在 k 国间轮换 | `rl/sandbox.py` |
| ✅ | ★★ **"几个国家"不再进观测形状**：军队 token 归属段从「甲/乙」两位改成**与网格同一套的六类** ⇒ 多玩家落地**不废基座**（§12.6 那条次序警告就此消失） | `rl/vocab.py`、`rl/encode.py` |
| ✅ | ★★ **"敌人"从"一个"改成"集合"**：`enemies_of` 转发 `evaluate.rival_nations`（**只有一份实现**）；`foe_*` 全部改成集合聚合 | `rl/encode.py`、`rl/evaluate.py` |
| ✅ | ★★ **击杀改成累计事件计数**（用户：「只计算我军杀掉的敌军来加分」）—— 修掉"数**看得见的**敌军"那个**迷雾悖论**（侦察到敌人反而扣分） | `rl/sandbox.KillLedger`、`rl/evaluate.py`、`vocab.GLOB.my_kills` |
| ✅ | ★ **防偷看的结构闸**：`score` 的 `mask` 仅限关键字 + **`enemy` 拒收集合**（`score(w,me,mask)` 会被静默读成全知 ⇒ 现在当场 `TypeError`） | `rl/evaluate.py` |
| ✅ | ★ **联赛池 = 5 份全新网络，每局随机抽 k 份不重复上场**（用户：「开局 5 个空权重，随机抽 pt 参与游戏」） | `rl/train.py` |
| ✅ | ★ **存档**（`--out`/`--ckpt-every`，原子写）+ **形状指纹**（`_shape_fingerprint`，加载时能判"过期了"） | `rl/train.py` |
| ✅ | ★ **对照线老师曾一步不走**（零代价目标陷阱 + 守家收缩是空操作）—— 修好，但**它不是基线**（见 §七 第 8 条） | `rl/military.py` |

### 12.9 ★ 定时重启（2026-09-25：不追内存增长，改为换进程）

**现象**：训练进程 RSS 跨 iter 涨（5 小时到 **2459MB / 3.3G 可用**）。
用户判断：「引擎是**单线程顺序**的…涨到这么多，**只能说明泄露**」。
★ 我查过两处模块级缓存，**都是有界的**（`combat_probs._POWER_CACHE` 键是`(兵种,撤退)`；
引擎 `_ADJ` 按地图尺寸、`grouping._STATE` 每局 `clear()`）⇒ 没找到明确的泄露点。

**决定**（用户）：「**算了，不如定时重启**」⇒ 不去追增长，改成**跑够 N 个 iter 换一个新进程**
（RSS 归零）并从存档续跑。实现：`--resume`（读档，**先校形状指纹**）+ `--restart-after N`
（跑完一段 `os.execv` 自己）。★ 用 exec 而非外挂 shell 循环：`run_ecs.sh` 只认 `rl.<模块>`，
而 exec 保持同一套 stdout/tee/日志管道不变。

★ **实测**：`--iters 4 --restart-after 2` ⇒ iter 1,2 → 重启 → 灌回 5/5 → iter 3,4 ✓；
线上换进程后 **RSS 2459MB → 309MB**，且**从 72 iter 续跑、一个没丢**。

### 12.8 ★★ 起点与病因（2026-09-25 用户纠正我三条，记下来别再错）

> 「你**第一次就用了 5 国大图**，当然难学」
> 「至于 oom，**我觉得不至于**」
> 「旧线**不是奖励稀疏问题，是状态空间爆炸**。**稀疏不是问题，alphazero 也稀疏**」

1. ★★ **病因是"状态空间爆炸"，不是"奖励稀疏"。** 我量到"势函数在动作层面几乎全平
   （间谍模式下只有 **2.7%** 的候选会让分数变）"，据此**错误地**把它讲成"这就是熵占主导
   的病因、要造每步信号"。用户纠正：**稀疏本身不是病**（AlphaZero 就是纯终局奖励）
   ⇒ **`min`→`平均` 那条建议已撤回**。那个 2.7% 只当**事实**记着，不当病因。
   ⇒ 本线对"状态空间"的回答是**结构性的**：候选屏蔽（K≈30，旧线 ~300）+ **视野局部窗口**
     （与地图大小无关）+ 六类归属 —— 这些都留着，是这一线存在的理由。
2. ★ **从小的起**（"第一次就 5 国大图当然难学"）。起炉改成 **8×8 / 3 国**
   （`n_nations_for(8)=3`，仍是"3 人起步"），大图**以后再加**
   —— 与 §七「训练从简单的开始**不代表模型只对标简单的**」一致：**课程式往上走**。
3. ★ **OOM 是"我配置的锅"，不是设计缺陷**（大图 × 500 回合 × 按步存整份观测
   ⇒ 单局缓冲区上 GB）。小图下缓冲区自然小 ⇒ **不必为此改设计**。

★★ **熵的可读参照系**（不量它，`ent=3.2` 这种数没意义）：**均匀分布的熵 = `log K`**。
实测 8×8/3 国：`K` 中位 **27**、均值 30.5（范围 4~128）⇒ **`log K` 均值 = 3.16**
⇒ **未训练的网 `ent ≈ 3.19`（比值 1.01）正好是纯均匀**（这是基准，不是发现）。
⇒ "**有没有学到策略**"的判据：**`ent` 能不能掉到 3.16 以下**（配合 `平均回合` 是否下降、
是否开始有网络稳定取胜）。

### 12.2 待做（按依赖排序）

1. ~~**"自己找厅"的探索信号**~~ ✅ **用户 2026-09-25 裁决：不做** —— 原话：
   「**探索信号不需要，我觉得模型会自己找，因为他会倾向于更大地盘，早晚会发现到厅的，
   只要有一个好基座就行**」。⇒ 12.6 那三条候选（热启动 / novelty / 朝敌国塑形）
   **全部作废**；不加任何奖励通道，靠**基座 + 扩张倾向**自己撞上敌厅。
2. ~~**多玩家**（3 人起步）+ ≥3 轮流手 + `n_nations = f(size)`~~ ✅ **已完成**（12.1'）。
   ★ 形状那条副作用**已经消掉**：观测**与国家数无关**了（归属走六类）
   ⇒ 以后再加国家也**不用重炼**。
3. ~~**炼基座**~~ ✅ **已在跑**（用户：「**只要有一个好基座就行**」）—— 起炉配置见 §12.7，
   ★ 但**实际跑的是小图版**（§12.8：用户纠正我"第一次就 5 国大图当然难学"）：
   **8×8 / 3 国 / 上限 500 / 池子 5 / 每 5 iter 定时重启**。
   实测到 **iter 77**：`ent` 3.10→1.8~2.6、平均回合 24→7.5 ⇒ **在学**。
4. **联赛池的"只增不删"那一半**（用户 2026-09-25 的「开局 5 个空权重、随机抽 pt
   参与游戏」＝**池子的起始形态，已做**）—— **剩下的是**：把**历史 ckpt 加进池子**
   （谁加、什么时候加、只增不删、多风格分化）。★ 用户 2026-09-25：
   「剩下内容先纳入计划，暂时不做」。
5. **批量操作**（可选）—— 用户：「**最多允许**模型批量操作，类似 llm 线输入数组」。
6. ⚠ **训练吞吐**（见 12.5，**待用户拍**）—— 用户 2026-09-25：「速度不重要，跑起来再说」
   ⇒ 暂不做。
7. ~~**`W_TILE` 的敌方那一半**~~ ✅ **已解决**（2026-09-25，用上面 (a) 那条）——
   用户的总口径把这件事直接定了：「**每个打分器只对自己国家负责**……**甲打了一块地，
   甲自己的计分器加分，乙的扣分**」⇒ `W_TILE` **只留"我的国土"**，敌方那半删掉
   （那正是"重复计"：甲一次拿两分）。以下旧分析保留备查 ——
   —— 用户 2026-09-25 已把「敌国军队数」与「敌方血量」两项改成**累计事件计数**
   （「只计算我军杀掉的敌军来加分」＋「`W_HP × (我的血 − 看得见的敌方血)` **也要改，
   和击杀一样**」），而那两项的病源是同一个：**只数看得见的 ⇒ 迷雾悖论**
   （侦察到敌人/敌国领土**反而扣分**，而"丢视野"反而涨分）。
   ★ **还剩** `W_TILE × (我的国土 − **看得见的**敌国国土)` 里的敌方那半：
     实测 `mask=全图` 与 `mask=空` 差 **`W_TILE × 10`**（比原来 `W_HP` 那处大一个量级）。
   **三种改法**（都自洽，**等用户拍**，别自己挑）：
     (a) **去掉敌方那半**，只留"我的国土"—— ★ 这与用户**自己**在厅那里的论证
         **逐字一致**：「打下对手的厅 ⇒ 那格 `owner` 变成我 ⇒ **我的厅 +1 已经记过了**，
         再从对手那侧扣一次等于同一件事记两遍」。夺敌国地块同理（我的国土 +1 已记）。
         而且用户原始的规格（§3.4）写的就是「**越多地分越高、丢地扣小分**」——
         **只讲我的地**，`− 敌方国土` 那半是后来加的。
     (b) 也改成**累计事件**（"累计夺取的敌国地块数"）—— 要新加一个领土账本。
     (c) 敌方那半**不过 mask**（全知）—— ⚠ 那是**偷看**（观测是 POMDP、只有厅公开），
         等于把迷雾拆掉，与用户"打分只对可见视野打分"直接冲突。**不推荐**。

### 12.3 ★★ 两个"静默失效"级的坑（本轮实测出来的，别再踩）

1. **`cross2` 是 O(K²)，必须分 minibatch** —— `ppo_update` 原来把整条 iter
   （~1800 步、K 可达 200）**一次性**前向+反传 ⇒ 注意力中间量顶到 **14 GB RSS**
   ⇒ 在 16 GB 的 Pi 上被 **OOM 杀**（`exit=137`，**日志只写到表头就断**）。
   ★ 这正是之前"ECS 炉子日志 0 字节 + tmux 整个消失"的**同一个病** —— 当时以为
   "证据不足"，其实一查 `journalctl -k | grep oom` 就有。
2. **`REWARD_TANH_SCALE` 必须有量纲意义** —— 它原来是 20，于是
   `tanh(100/20)`、`tanh(200/20)`、`tanh(500/20)` **全是 1.0**
   ⇒ **厅值 100 还是 500 在奖励上完全一样**（用户调了等于没调），连"10 个步兵"和
   "2 个步兵"都分不开。现在锚到 `W_HALL_IN_INFANTRY × W_ARMY`（= 一座厅）。

### 12.4' ★★★ 打分的**总口径**（2026-09-25 用户定的，覆盖下面 12.4 的每一条）

> 「你记账到底是怎么写的，**每个打分器只对自己国家负责**就行了，例如**甲打了一块地，
>   甲自己的计分器加分，乙的扣分**，**完全不需要什么视野地图**」
> 「**除了威胁系统**，其他的都和这个一个口径」
> 「打分器这玩意也就是**早期用用，未来还要拆掉的**，打分器单纯是为了**控制熵**，
>   **代替 bc 先验**，**学会之后只看胜场**」

**把它当宪法用**（改任何一项先套这两条）：

| 规矩 | 意思 |
|---|---|
| **只对自己国家负责** | 一切项只数**我自己的量**（我的国土/军/血/战果/我离敌厅多近）。**不许**把"对手的损失"再记一遍 —— 那是**对手自己那本账**的事 |
| **其余不许读敌人** | 除了威胁系统，任何项都不许读敌军的**当前位置/数量**。敌**厅**的位置**可以**（那是永久公开事实，走记忆账本） |
| **威胁系统是唯一例外** | 且它读敌军位置是**正确**的：「暴露出的敌人越多防御越有价值，**没暴露的也无法虚空防守**」⇒ 迷雾门控在这一项上不是毛病 |
| ★ **它是脚手架，要拆** | 它是**纯势函数**（拆掉不改变最优策略）；目的是**控熵 + 代替 BC 先验**；**最终只看胜场**。⇒ 别为它做精巧设计，但也**绝不能教错东西**（学坏的习惯拆不掉） |

**这一口径逼出来的三处修改**（都是"重复计 + 迷雾悖论"）：
1. `W_TILE`：`(我的国土 − **看得见的**敌国国土)` → **`我的国土`**。
   甲打下一格 ⇒ 原来**甲一次拿两分**（我 +1 且敌 −1），而"乙扣分"本该记在**乙**的账上。
2. `W_NEAR` 逼近：`(−我离敌厅 + **敌离我厅**)` → **`−我离敌厅`**。后半截是**对手自己**
   账里的 `−my_d`，放进我的分里就是同一件事记两遍。
3. `W_GUARD` 守家加成：**停用**（用户：「其实**扣分就行了**」）—— 它会变成无条件的
   "缩在核心"收益；威胁本身已经提供回防压力。常量留在先验表里（表只许追加/停用）。

★ **实测验证**：用真实路径（厅位置来自永久账本）⇒ **间谍/自己找厅两种模式、`mask` 空
vs 全图，分差都是 `0.000000`** ⇒ 除威胁外**确实零视野依赖**。测试
`TestNoFogDependence` 钉住（并带反向对照：威胁项必须**还能**响）。

### 12.4'' ★★★ 观测里**删掉一整批"对手标量"**（2026-09-25）

用户：「`foe_tiles`/`foe_armies`/`foe_cap`/`foe_hp_frac`/`foe_moved` 这些是什么？
**有没有超越真玩家的内容**」→ 查实：**五个全是上帝视角**（一次 `mask` 都没过）
→「**全都不要了**，而且这个规则是**训练的规则**，实际如何补员**由 llm 决定**，
未来这些规则都会**情景化**，**根本不是什么该计入的规则**」。

⇒ **整批删除**（不是加 mask；GLOB **28 → 22**）。删的是**两条病因**：

| 病因 | 具体 |
|---|---|
| ① **超越真玩家** | 不侦察就知道对手的**国土总量/军队总数/总血量**；`foe_moved` 更是**战术级**泄露（「他们全军都动过了 ⇒ 现在打他」） |
| ② ★ **在数"训练用的规则"** | `*_cap` = `5 + 国土//10` 是**沙盒的补员公式**。真实对局里补员是 **LLM 自己的决策**、规则将来还要情景化 ⇒ 喂进去等于教模型**沙盒规则**，不是**战局** |

★ 连 `my_cap` 一起删（同一条规则的我自己那一半）；`my_hp_frac` 的归一改用
`scoring.HP_REF_ARMIES`（**只是个尺度，不是规则**）。
★ 留下的 `my_*` 全是**我自己的真实状态**（国土/军数/血量/能动几支）—— 全知是对的。
★ 留下的 `foe_*` **只许是公开的国祚**（存活 + 位置：亡国是公开事件，厅位置走记忆账本）。
★ 守卫：`test_no_foe_scalars_and_no_rule_derived_caps` 钉住"这几列不许回来"。

⚠ **教训（我犯的）**：我一度把这批列改成"过 mask 只数看得见的" —— 那是**答错了方向**：
问题不只是"读得太全"，而是**这些量本身是训练规则推出来的**，过不过 mask 都不该进观测。
★ 同批还修掉我引入的一个顺序 bug：用 `mask` 的代码被写在 `mask` 归一化**之前**
⇒ `mask=None` 调用会 `TypeError`。

★★ **`turn_frac` 也删了**（GLOB 22 → **21**）。我一度为它辩护说"回合上限是引擎里的真实规则
（`max_turns` 缺省 200）"，用户当场纠正：「**真游戏是无限回合的**，你加这玩意干嘛」
⇒ `turn_frac = 当前回合 / 兜底上限` 的分母是**训练为了防僵局才定的**，不是游戏规则
⇒ 与 `*_cap` **同一类**，删。★ 删掉之后模型**没有"时间感"是对的**：无限回合的游戏里
"打到第几成了"本来就不存在，该看的只有**相对强弱**。

★ **现在的 `GLOB`（21 列）里只剩两类**：① 我**自己的真实状态**（国土/军数/血量/能动几支/
战果）；② **公开的国祚**（存活 + 位置，走永久记忆账本）。归一化的分母都是**尺度**
（8 / 4 / 边长² / 100×8），**不是规则**。

⚠ 还留着的一处**训练痕迹（在奖励里，不在观测里）**：`done()` 的 `turn >= t_max` 判平、
`reward() = 1 − turn/t_max`（"越快赢越高"是用户 09-24 自己定的）。无限回合的游戏里
"平局"这个终止条件本身是训练为了收敛才加的 —— **没动**（用户定的口径 + 需要上界），
记在这里备查。

### 12.4 打分的口径（本轮被用户逐条问准的，**别再自己发明**）

· **分数是针对自己的**：`+ W_HALL × (我的厅 + 0.5×盟友的厅)` —— ★ **对手的厅不进分数**。
  我上一版写了 `− 对手的厅`，用户一句「**和对面几个厅有半毛钱关系？**」——
  两处错：① **重复计**（引擎实测：打下对手的厅 ⇒ 那格 `owner` 变成我、厅保留 ⇒
  "我的厅 +1"已经记过了）② **迷雾悖论**（只数看得见的厅 + 势函数取差分 ⇒
  **侦察到对手的厅反而扣分**，"去找厅"成了负收益）。
· **厅 = `W_HALL_IN_INFANTRY` 个步兵**（当前 20 ⇒ ±200；盟友 ±100）。
· **距离/威胁/守家对每一座厅都生效**（不是只取第一座）。
· **终局 = 实体口径**：只剩一个外交实体 ⇒ 它赢（**联盟胜利或单国胜利**；
  我战死但我的盟赢了**也算我赢**）。
· **盟友**：走**同一个 `_one`**（去掉地皮分）× **50%**。
· `halls_known=True`（缺省）＝"已派间谍"：★ **"知道厅在哪" ≠ "看得见厅上的守军"** ——
  是**独立开关**，**不能**靠"把厅格塞进 `vision_mask`"（那会连守军一起暴露，是偷看）。
  ★ 厅**也不进网格**：网格 = 视野外接框，框外的格**没有位置**（实测）⇒ 走**相对坐标**
  放进 `GLOB`（与军队 token 同原点 ⇒ 模型能用 cross-attention 算"哪支军离厅最近"）。

### 12.5 ★ 战斗明细进观测（本轮做的，三条口径 + 一条实测）

**接进哪里**（三处，同一帧算一次，`obs_of` 往下传）：

| 位置 | 内容 | 为什么在这 |
|---|---|---|
| **军队 token 尾段**（`vocab.A_EXTRA` 14 列） | 这支军**自己那一方**的 `P(赢/输/同归/占地)` + **轮数累积分布** + 加增援后 `P(赢)` + 期望轮数/掉血 + **撤退保命** | ★ **与视野无关**（我军恒在 token 里）⇒ 大地图上也是活的；撤退保命**逐军不同**，只有这里放得下 |
| **网格**（`vocab.GRID_CB_*` 13 通道） | 同上（**我的视角**） | 候选按目标格 gather ⇒ "我要去的那格正在打什么仗"一眼读到 |
| **全局 token** | **不加** —— 骰子表/撤退常数已在 `features.glob_rule_vector()` | 分布已由概率通道表达，见下 |

**三条口径（都踩过或被实测逼出来的）**

1. ★★ **骰子不进观测**：引擎 `_die()` 在 `_resolve_battles` 里**掷完即弃、不落状态**
   （`mp.py:1863`）⇒ "上一回合的骰面"在引擎里**不存在**（要它就得改 `mp.py`，
   而本线引擎与 main 逐字一致）。**分布**就是概率通道本身（概率=对骰子积分）。
   ⇒ 这条路到此为止，别再找。
2. ★★ **接触即看见**（`scoring.CB_CONTACT_VISION`，默认开）：实测引擎视野
   = **自家/盟方地块及八邻**（`pathfind.vision_mask`），**不含"我军自己站的那格"**
   ⇒ 交战格可见率 **8×8 只有 27%~66%、16×16 只有 0%~7%**（有一局 1202 个样本**一个都不在视野里**）。
   死守 `visible_to` 的话这个特征在大地图上**等于没做**。口径："我的军正在这格上打"
   ⇒ 这格的战斗明细算得出来（仗打在我身上；`mp.py:1823` 的 `_resolve_battles`
   **根本不看视野**，引擎自己就把"打起来了"当无条件事实；引擎还用错误消息
   主动公开接触：「(5,5) 有敌军驻守…**进攻请用 atk**」）。★ 它**只**决定
   "哪些格能写概率"，**不扩视野本身**（网格其余通道、敌方 token、增援过滤一律照旧走 `mask`）。
3. ★★ **增援必须过滤可见性**：`reachable_reinforcements` 默认遍历 `world.armies`
   **全表（含看不见的敌军）** ⇒ 观测路径一律传 `allow`。`frame_odds` 把 `mask` 设成
   **必填位置参数**，就是为了让"忘了传"直接 `TypeError`（不给静默偷看留路）。
   ★ 测法要**双向**：先证明"不过滤时确实变差"、再证明"过滤后一样" ——
   只测后者的话，增援压根没起作用时那条测试也是绿的（假测试）。

**一条实测（性能，**未优化**）**：战斗明细 `8×8` 约 **7 ms/步**、`16×16` 约 **20 ms/步**
（交战格样本上 `assess` **16~76 ms/个**、加增援再一份），而模型前向只要 **8 ms**
⇒ 观测比前向贵。根因是 DP 本身（`rec` 每次 `assess` 调 ~2400 次，`n_states` 中位只有 7、
**从不触顶也不截断**）。★ 有一个**精确**的省法（**没做，等用户拍**）：同一回合内
战斗状态**通常不变**（每回合只结算一次），按 `Battle` 的**内容签名**做 memo
（不是 `id()`！不是"跳过重算"）能把 DP 次数掉一个量级。用户此前说过「别优化速度了」，
所以**先报数、没动手**。

### 12.6 ★★ 市政厅的**永久记忆**，与"自己找厅"的探索问题

**用户口径**（2026-09-24，中途给的）：「**发现厅了就应该永久标记，因为厅是拆不掉
也不能移动的**」⇒ `rl/hall_memory.py`（`{格: 最后看见时的主人}`，**只增不减**）。

**为什么这条口径是硬需求**：引擎的视野是**逐帧**的，而"厅在哪"是**永久事实**
（`_conquer` 只换 `owner`，`buildings` 原样留着 ⇒ 厅谁都拆不掉、也不动）。
拿"当前视野"回答"厅在哪"，两个后果**都不报错**：
① **观测闪断** —— 同样知道的事，一帧读得到、一帧读成 0；
② **打分器闪断** —— 逼近/守家要读敌厅位置 ⇒ 敌厅一离开视野那几项**当帧塌 0**，
势函数差分变**噪声**（本该是"我看见过它，它一直在那儿"）。

**两种模式收敛成一个机制**：`halls_known` 那个 bool **从 `evaluate`/`encode` 里消失了**，
只剩沙盒构造时的**记忆初值**：间谍 = 开局账本装满（`all_known`），自己找厅 = 从空累积。
调用方一律问 `sb.known_halls(name, mask)`（顺手把本帧看见的记下来）。

**★ 顺带修掉一个真 bug**：「逼近」和「守家」原来塞在**同一个** `if my_halls and foe_halls:`
里 —— 而 `foe_d` 走的是 `min_dist(world, enemy, …)` = **敌军**到我的厅的距离，
**跟敌厅在哪儿毫无关系** ⇒ 自己找厅那一版里，**敌厅没找到之前"守家"一分没有**。
用户加这两项就是为了让"回防"有收益（「5 支军队赖着主城不动压根输不了，是打分模型
**没有奖励防御**」），而那正是这一版最需要的东西。现在拆成两块，各判各的。
★ 拆的时候**没顺手放偷看进来**：`test_proximity_still_needs_both_halls` 钉住 ——
敌厅看不见时，把军摸到敌厅边上**分数一字不变**。

**★★ 实测出的新问题（待用户拍）**：随机策略在"自己找厅"那一版里**整局看不到敌厅** ——

| 地图 | 6 个种子里有几个看到过敌厅 | 首次看到 |
|---|---|---|
| 8×8 | **2 / 6** | 第 20 回合 |
| 16×16 | **0 / 6** | 从未 |
| 24×24 | **0 / 6** | 从未 |

⇒ 永久记忆**只解决了"找到之后"**；**找到之前没有任何梯度指向敌厅**
（`W_NEAR` 要两边都已知 ⇒ 死的；`W_THREAT`/`W_GUARD` 只管守家）。
这一版要学"找厅"，得先有一个**探索的理由**。三条候选（**未选**，等拍）：
1. **从已知版基座热启动** —— 基座已经会"为厅而战"，切到找厅模式只差侦察
   （计划里本来就写了「基座先跑通再上」；代价：两份 ckpt 的观测量纲一致，可直接续）；
2. **发现新厅给一次性奖励**（novelty bonus，加一个先验表开关）；
3. **给"朝敌国方向推进"一点塑形**（覆盖率/前沿距离之类）—— ★ 最容易变成
   "奖励走路"的坑，最后选。

★★ **2026-09-25 用户裁决：探索信号不做** —— 原话：「**探索信号不需要，我觉得模型
  会自己找，因为他会倾向于更大地盘，早晚会发现到厅的，只要有一个好基座就行**」。
  ⇒ 上面三条候选**全部作废**，`W_NEAR` 死了就让它死着；不加 novelty、不加方向塑形、
  不走"两份 ckpt"的热启动。**"自己找厅"这一版靠基座 + 扩张倾向自己撞上敌厅。**
  ★ 代价要认：撞上的概率低（上表），所以这一版**学得慢是预期的**，不是 bug。

★ **盟友的厅也纳入标记**（用户 2026-09-25：「顺便**盟友发现厅应该也纳入标记**」）：
  查实后 `HallMemory.observe` **本就按格记账、不看归属**（盟友的厅一直在册），
  `encode_glob` 的 ally 段也本就走 `known`；★ **只有 `evaluate.score` 是全知口径**
  （`halls_of(world, al)` 没传 mask/known）⇒ 同一局面观测说"0 座盟友厅"、打分器说"1 座"。
  已归到**同一本账**。结构上当前等价（`vision_mask` 把联盟成员的**地块**也算进视野），
  所以**不改数值、改机制**：联盟/视野口径将来一变，打分器跟着走而不是静默说谎。
  ★ 顺带查出一处**真闪断**：`encode_grid` 把厅那一段塞在 `if not visible: continue`
  **下面**，判据里的 `or (x, y) in known` 于是成了**死代码** —— 网格 = 视野外接框
  （**矩形**）而视野不是 ⇒ 框内有一圈看不见的格，一座**记得的**敌厅落在那一圈里时
  `GRID_HALL_*` 当帧塌 0，而同帧 `GLOB foe_hall_d` 还在（实测证实）。已抬到 `continue` 之前。

★ 与 **#10 多玩家的次序**：多玩家会改 `NATION_SLOTS` ⇒ **观测形状变** ⇒ 现在炼的
  基座到那时作废。⇒ **先做 #10、再炼基座**（用户 2026-09-25 说"只要有一个好基座就行"
  ⇒ 基座是下一件大事，正因如此更不能让它白炼）。

### 12.7 ★ 起炉配置（2026-09-25，ECS，通宵）

用户 2026-09-25 逐条定的口径：

| 项 | 值 | 出处 |
|---|---|---|
| 国家数 | **3–5**，由 `n_nations_for(size)` 定（8–20⇒3、24⇒4、28+⇒5） | 「随机地图，**3-5 个国家**，随机大小」 |
| 地图 | **随机大小**，起炉用 `--size-min 8 --size-max 32`（这样 3/4/5 国都覆盖到） | 同上 |
| 外交 | **默认不结盟、默认无中立**（全对宣战） | 同上 |
| 厅 | **已知**（间谍模式 = 基座） | 用户 2026-09-04「先炼一个基于已知的**基座**」 |
| 回合上限 | **500**（原来是 200） | 「**对局最大 500 回合**」 |
| 池子 | **5 份全新网络，随机抽 k 份上场** | 「开局 **5 个空权重**，随机抽 pt 参与游戏」 |
| 速度 | **不优化** | 「速度不重要，跑起来再说…慢无所谓，挂一晚上通宵跑」 |

★ 起炉命令（`rl/run_ecs.sh`，单线程 + tmux + 日志落盘）：

```
./rl/run_ecs.sh train --iters 1000 --episodes 6 --t-max 500 \
    --size-min 8 --size-max 32 --pool 5 --halls-known \
    --out rl/runs/ppo/base_$(date +%m%d).pt --ckpt-every 2
```

★ **实测的吞吐事实**（供将来判断"跑得值不值"）：每步 ≈ **10~12 ms**
（观测 1.5~3.4 ms + 前向 ~9 ms；观测**便宜**，与 §12.5 那条"战斗明细 7~20ms"
不矛盾 —— 那条是**有交战**时的峰值，开局无交战所以只有 1.5 ms）。
★ **真正的成本是"局有多长"**：随机策略下一局 **1000~7500 步**（12×12 直接打到兜底），
所以早期每局几分钟，**学会进攻后局会大幅变短**（旧线 51→5.8 回合）。


## 十、两条纪律（来自旧线的教训）

1. **报数要说清覆盖到哪一段**（用户 09-18）。
2. **每个自动闸门都要故意破坏一次确认它会响**；**别用 `git add <目录>` 就以为加全了**
   —— commit 前看**全量** `git status --short`（2026-09-24 两次踩）。