# 2026-09 实验产物归档（09-18 ~ 09-20）

> 这些是**从 `/home/northwind/.claude/jobs/556a2faa/tmp/` 抢救出来的** ——
> 那个目录是 Claude 会话的临时工作区，**随时会被清理**，而它们在里面是唯一副本。
> RL 线封存（2026-09-24）时复制到这里。原始目录的 `.pid` 与 0 字节文件没抄。
>
> ⚠ **2026-09-24 补记**：本目录原先是放在 `rl/runs/archive_2026-09/` 的，而 `rl/runs/` 在
> `.gitignore` 里 ⇒ 那一次 `git commit -a` **没把这 22 个文件收进 git**（唯一副本仍裸着）。
> 已挪到 `rl/archive_2026-09/`（不在 ignore 区）并真正入库 —— 你看到的这个路径就是修好后的。
> 同一批的代码改动**已经在 `feat/rl` 上**（那个 worktree 整体落后于分支，无独有内容）。

## ★先看这一条

**`bc_ep400.pt`（9.1 MB）** —— PPO 那两炉的**锚模型**（`ppo_candx` / `ppo_exec` 的
`config.json` 里写着 `bc_anchor_ckpt: rl/runs/bc_ep400.pt`），而**那个路径不存在**
（从没归档进仓库）⇒ **这里是全盘唯一副本**。

## 各文件是什么

| 文件 | 是什么 | 关键数 |
|---|---|---|
| `gd.jsonl` / `gn.jsonl` | `--grad-diag` 的逐块输出（iter 116 / 171） | **`coh(pg) = 0.0635`**，≈ 噪声底 `1/√230 = 0.0659`；其余三项 `vf 0.513` / `ent 0.653` / `bc 0.317`。**`cos:pg:bc = −0.778`**（pg 与锚互相拆台） |
| `exec_sweep.log` | 可执行性头 + β 扫描 | 头 AUC `0.382 → 0.979`；β 扫描见下表 |
| `kind_beta.log` | β=0 vs β=1 的**逐类型尝试/成功表** | `build` 成功率 **7% → 73%**；`move`/`attack` 仍 100%（**没被误压**）；`sell 64→79%`、`recruit 13→87%`、`buy 67→95%` |
| `kind_beta1.log`、`exec_head.log` | 两个探针**未跑完**的输出（只有表头） | — |
| `invalid_rate.log` | 撞墙率分解（4 个 ckpt，采样臂） | BC 起点 `1.7 次/回合`（罚占比 5%）→ PPO 后 **`7.9 ~ 10.6`**（罚占比 **21~25%**）；被拒构成以 `build` 为首（995~1764 次） |
| `two_arms.log` | 同上但**贪心臂** | 地：BC 起点 16 > PPO 的 11~12 |
| `endturn.log` + `endturn.sh` | `ppo_exec` 末（`ckpt_185`）的 β=0/1 对照 | 撞墙 `9.0 → 3.5`、罚占比 `25% → 13%`；但消费 `10,691 → 9,422`、地 `23 → 13` |
| `reasons.py` | 被拒原因分解工具（输出到 `reasons_turn.log`，那次是空的） | — |
| `diag_leak.py`、`verify.py`、`mini.py`、`zz_bisect_mods.py`、`verify.out` | 09-18 排查**测试污染**（`TestJitterRegression` 漏快照 `game.MARKET`，即 `d058ec3` 修的那条）用的一套小工具 | `verify.out` = 「全前缀都不触发」 |
| `graddiag_pi115.log` | `--grad-diag` 那炉的开头（从 `ppo_candx/ckpt_115` 续，锚 = `bc_ep400.pt`） | — |

## β 扫描（`exec_sweep.log`，单图 900000，采样臂）

| β | 被拒率 | 消费 | 地 |
|---|---|---|---|
| 0.00 | 54.1% | 10,378 | 27 |
| 0.50 | 14.7% | 9,238 | 24 |
| **1.00** | **10.8%** | **10,856** | **29** |
| 2.00 | 7.8% | 9,914 | 24 |
| 4.00 | 12.8% | 7,978 | 14 |

⇒ **β=1.0 是甜蜜点**（被拒率砍 5 倍，消费与地都没掉；β=4 崩）。
`ppo_exec` 那炉取 `exec_beta=1.0` 有这个依据，不是拍的。

## ★★口径警告（会踩）

**这些数全是单图 `seed 900000`**，**不是** `probe_anchor_eval.py` 的 5 张留出图
（900000~900004）取中位。**不能与 `rl/PLAN.md` 里 09-18 那组（5 图中位）混比** ——
例如本目录里 `bc_ep400` 的消费是 12,411（单图），而 PLAN 里 BC 起点是 8,537（5 图中位）。

## ⚠ 还带着一个"工具骗人"的现场

`endturn.log` 末尾那段 traceback（`FileNotFoundError: '1.0'`）与
`exec_sweep.log` 末尾的 `FileNotFoundError: '0,0.5,1,2,4'` 是**同一个坑**：
`--beta 1.0` / `--sweep 0,0.5,...` 的**值被当成 ckpt 路径**去 load。
而 `endturn.sh` 里那句 `|| echo "★★ β=1 超时未完成 —— 就是它卡"`
**把"崩溃"印成了"超时"** —— 于是当时的结论是错的（详见 `a526ccc` 的 commit message）。
这两个 log 是那件事的原始现场，留着当教材。