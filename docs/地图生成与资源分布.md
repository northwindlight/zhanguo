# 地图生成与资源分布（人类侧资料）

> ⚠ **本文件的内容不要搬回 README、也不要注入任何局内 AI 上下文。**
> 地形分布/资源概率属于**上帝视角**：局内 AI 能合法获得的只有地块**地形**（纯函数、
> 面板可见）与**自家已占地块**的资源数，写进局内上下文就破坏了"他国国情只能靠侦察
> 与推断"的迷雾设计。
> （2026-09-15 之前这条更要紧：匈奴的 `rules` 会附 **README 原文全文**，"README 里能写
> 什么"直接等于"会不会喂给局内玩家"。现在 `rules` 一律返回**代码现算**的规则文本、
> 所有政体同一份，局内上下文不再读 README——但"分布明细留在本文件"这条不变：
> README 是给人看的总览。）
>
> 数字全部由 `game.py` 的权重表现场算出（`TERRAINS` / `TERRAIN_WEIGHTS`），与引擎同源。
> （2026-09-15 起 README 不再承载数值，原同步守卫已退役；本文件仍是分布的唯一出处，勿搬回 README。）
>
> 规则的完整说明书（回合与结算、经济、建筑、军事、外交、联盟、市场、命令表）见
> [游戏说明书.md](游戏说明书.md)——那份文件**特意不重复地形**，地形属性以本文件为准。

## 生成机制（2026-09-15 换过：蓝噪声 + 密度图调制）

**旧法**（逐格 i.i.d.）已被替换：`random.Random(f"{seed}:{x}:{y}")` 逐格独立抽是**泊松随机场**——
局部既结块（"一地金子"）又有孔洞，且"起步 5 格开什么牌"会被复利放大。

**新法**（`mapgen.py`，无第三方依赖）：

- **低频场**：哈希值噪声 + fBm 两张场（海拔 / 湿度），特征尺度 `MAPGEN_SCALE`；
- **蓝噪声 rank**：多趟 dart throwing（占用网格提速），每格一个 rank ∈ [0,1)，
  **局部均匀**（摊开、不成块、非规则格）；
- **地形落位**：密度场给每类打分 → 按 `TERRAIN_WEIGHTS` 配额**稀有类型先挑**、同分由 rank
  抖边界 ⇒ **该成片的成片（山脉/林带/沙带），片内不结块**；全局占比**精确**等于配额；
- **资源落位**：每类资源一张**独立**蓝噪声掩码，按该地形的权重表**抖动阈值**取档
  ⇒ **每格边缘分布与旧法逐字相同**（下方期望/档位数字仍然成立），变的只是
  "高值格在空间上摊开"（黄金贴邻比 0.549→0.381、最大簇 6.2→3.4、孔洞 5.08→4.47）。
- **契约变化**：地图现在是 **`(seed, size)` 的函数**（不再是 `seed:x:y` 的逐格纯函数）——
  同 seed 换尺寸会是另一张图；**旧存档按 `SAVE_VERSION` 拒载**（已物化格的 terrain/resources
  是字面量存在档里的，不拒载会出现"已占格=旧算法、新占格=新算法"的混图）。
- 地名：`random.Random(f"{seed}:{x}:{y}:name")`（未变；撞名重试只影响名字本身，不碰战斗随机流）。
- 野人守卫：开局全图无主格每格一支（100HP），是地图静态属性；死后永久清空（未变）。

跑法：`python3 mapgen.py [size]` 打印生成耗时与各地形占比；打开方框见 `tests/test_mapgen.py`。

## 地形先验权重

| 地形 | 权重 | 占比 | 防御 | 建设惩罚 |
|------|-----:|-----:|-----:|-----:|
| 平原 | 30 | 30% | +0% | +0% |
| 森林 | 25 | 25% | +10% | +15% |
| 丘陵 | 20 | 20% | +25% | +25% |
| 山地 | 15 | 15% | +50% | +50% |
| 沙漠 | 10 | 10% | -10% | +40% |

## 资源档位权重表（`TERRAINS`）

每格：下标 = 资源数值（x0, x1, …），元素 = 抽中该数值的权重。

**矿石**（实际最大档 x5）

| 地形 | x0 | x1 | x2 | x3 | x4 | x5 | 期望 |
|------|---:|---:|---:|---:|---:|---:|---:|
| 平原 | 80 | 16 | 3 | 1 | 0 | 0 | 0.25 |
| 森林 | 74 | 19 | 6 | 1 | 0 | 0 | 0.34 |
| 丘陵 | 18 | 28 | 25 | 17 | 8 | 4 | 1.81 |
| 山地 | 4 | 18 | 29 | 26 | 16 | 7 | 2.53 |
| 沙漠 | 68 | 21 | 8 | 3 | 0 | 0 | 0.46 |

**黄金**（实际最大档 x2）

| 地形 | x0 | x1 | x2 | 期望 |
|------|---:|---:|---:|---:|
| 平原 | 96 | 4 | 0 | 0.04 |
| 森林 | 95 | 5 | 0 | 0.05 |
| 丘陵 | 88 | 11 | 1 | 0.13 |
| 山地 | 76 | 20 | 4 | 0.28 |
| 沙漠 | 89 | 10 | 1 | 0.12 |

**耕地**（实际最大档 x5）

| 地形 | x0 | x1 | x2 | x3 | x4 | x5 | 期望 |
|------|---:|---:|---:|---:|---:|---:|---:|
| 平原 | 6 | 26 | 32 | 21 | 11 | 4 | 2.17 |
| 森林 | 28 | 30 | 26 | 11 | 4 | 1 | 1.36 |
| 丘陵 | 32 | 30 | 24 | 10 | 3 | 1 | 1.25 |
| 山地 | 72 | 20 | 6 | 2 | 0 | 0 | 0.38 |
| 沙漠 | 90 | 7 | 2 | 1 | 0 | 0 | 0.14 |

**石油**（实际最大档 x4）

| 地形 | x0 | x1 | x2 | x3 | x4 | 期望 |
|------|---:|---:|---:|---:|---:|---:|
| 平原 | 92 | 7 | 1 | 0 | — | 0.09 |
| 森林 | 95 | 4 | 1 | 0 | — | 0.06 |
| 丘陵 | 80 | 15 | 4 | 1 | — | 0.26 |
| 山地 | 74 | 20 | 5 | 1 | — | 0.33 |
| 沙漠 | 12 | 26 | 28 | 26 | 8 | 1.92 |

**木头**（实际最大档 x5）

| 地形 | x0 | x1 | x2 | x3 | x4 | x5 | 期望 |
|------|---:|---:|---:|---:|---:|---:|---:|
| 平原 | 37 | 30 | 21 | 9 | 3 | 0 | 1.11 |
| 森林 | 5 | 15 | 29 | 28 | 17 | 6 | 2.55 |
| 丘陵 | 24 | 29 | 25 | 14 | 6 | 2 | 1.55 |
| 山地 | 60 | 26 | 11 | 3 | 0 | 0 | 0.57 |
| 沙漠 | 95 | 4 | 1 | 0 | 0 | 0 | 0.06 |

## 全图每格期望（按地形先验加权）

| 资源 | 期望/格 | 档位范围 |
|------|-------:|---------|
| 矿石 | 0.9475 | 0–5 |
| 黄金 | 0.1045 | 0–2 |
| 耕地 | 1.3120 | 0–5 |
| 石油 | 0.3355 | 0–4 |
| 木头 | 1.3720 | 0–5 |

2026-09-09 重平衡记录（`game.py` 注释同源）：黄金富矿（x2）概率明显调低
（期望 0.147→0.105 座/格，−29%），石油略增（0.27→0.34，
沙漠仍是唯一宝地且多出一档 x4），矿/耕/木总量基本不变、地形专精更陡
（山地矿石 2.53、森林木头 2.55、
平原耕地 2.17、沙漠石油 1.92）。

> 注：`game.RESOURCE_MAX` 是死代码（全库无引用），且石油值 3 与沙漠权重表（可到 x4）不符——
> 档位以 `TERRAINS` 权重表长度为准。

## 空间均匀度（新旧对照，2026-09-15 实测）

> ★ **数字锚定**：下表测于 2026-09-15 地形改版提交（本文件所在提交，`git log -1 -- mapgen.py`）。
> 复跑：`python3 mapgen.py`（占比/耗时）+ `tests/test_mapgen.py` 的同一批指标（贴邻比/最大簇/分区 CV），
> 旧法基线仍可由 `experiments/gold_uniformity2.py` 的 A 列复现。

旧法用 `experiments/gold_uniformity2.py` 的 A 列（iid）量过：逐格独立抽是泊松点过程，
天然成簇——**数列均匀 ≠ 空间均匀**，分区变异系数恰等于泊松预测 1/√λ（大图 0.128 vs 理论 0.124）。
现在改用蓝噪声 + 密度图调制，同一批指标（12 seed × 50/60/80 三种尺寸）：

| 指标 | 旧 iid | 新（蓝噪声+密度图） | 读法 |
|---|---|---|---|
| 最大同类块·森林 | 38 | **72** | 成片（林带） |
| 最大同类块·山地 | 15 | **25** | 成片（山脉） |
| 最大同类块·沙漠 | 8.4 | **52** | 成片（沙带） |
| 贴邻比·黄金 | 0.549 | **0.381** | 高值矿点更不结块 |
| 最大簇·黄金 | 6.2 | **3.4** | 消灭"一地金子" |
| 距最近金矿最远 | 5.08 | **4.44** | 孔洞更少 |
| 分区 CV·黄金 | 0.801 | **0.681** | 区域更公平 |
| 最大簇·矿石 | 1682 | **845** | 那个盘踞半张图的巨矿碎了 |

指标定义与三方案对比实验（iid / 4×4 分区配额 / 蓝噪声）见 `experiments/gold_uniformity.py`、
`experiments/gold_uniformity2.py` 与 `experiments/README.md`；
新法的落地实现与不变量（配额精确、边缘分布不变）由 `tests/test_mapgen.py` 钉住。

## 采集建筑上限

地块某项资源量 = 该地该类采集建筑的可建座数上限（如金矿 x2 的地最多建 2 座黄金矿场）。
建筑造价/产出属于**游戏规则**（局内 `rules` 可查），在 README 建筑表；本文件只管分布。
